<% Object.entries(response).forEach(entry => {const [id,obj] = entry; const title = obj.title;
     %>
              <li class="nav-item">
                <a class="nav-link active" href="?use_layout=<%= id %>">
                  <span data-feather="home"></span>
                  <%= HCSR.transl(title,"products") %><span class="sr-only">(current)</span>
                </a>
              </li>
<% }); %>
