import type { UserId } from '../../../kernel/ids/index.ts'; import type { CreateLibrary, UpdateLibrary } from '../api/contracts.ts'; import type { LibrariesRepository } from './libraries-repository.ts';
export class LibrariesService { constructor(private readonly repository: LibrariesRepository) {} list(a: UserId, t: string) { return this.repository.list(a, t); } create(a: UserId, t: string, i: CreateLibrary) { return this.repository.create(a, t, i); } update(a: UserId, t: string, id: string, i: UpdateLibrary) { return this.repository.update(a, t, id, i); } async remove(a: UserId, t: string, id: string) { await this.repository.remove(a, t, id); return { removed: true as const }; } }
