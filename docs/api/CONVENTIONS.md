# Conventions API — module Gestion commerciale (Epic E10)

> **Statut : opposable.** Produit par la story **E10.0 « Socle API-first »**, Sprint 5.
> Source de vérité du contrat : [`openapi/magrit-core.v1.yaml`](../../openapi/magrit-core.v1.yaml).
> Exigence d'origine : API-first / modulaire posée par Xavier Péchoultres au WM du 2026-09-01 — l'intégration Magrit ↔ Studio démarre côté données à partir de ce contrat, pas à partir du code.

Le contrat n'est pas un sous-produit du code. Il vient **avant**. Un endpoint qui n'est pas décrit dans `openapi/magrit-core.v1.yaml` ne se code pas.

---

## 1. Les 13 critères d'acceptation et leur mise en œuvre

| # | Critère | Où c'est tenu | Comment le vérifier |
|---|---|---|---|
| 1 | `openapi/magrit-core.v1.yaml` (OpenAPI 3.1) fait foi ; aucun endpoint E10 codé sans y être décrit avant | Le contrat ; l'ancien `docs/architecture/api/openapi.yaml` porte un en-tête de dépréciation ; **registre `src/server/api/gescom-routes.ts`** | `pnpm test:contract` → « CA1 » du document **et** `gescom-routes.contract.test.ts`, qui exige pour chaque route enregistrée un `operationId` correspondant dans le contrat |
| 2 | Types TS générés depuis le contrat via `pnpm gen:api` ; aucun DTO écrit à la main des deux côtés | `scripts/gen-api-types.sh` → `src/platform/api/generated/magrit-core.v1.ts` ; `CONTRACT_ALIGNMENT` dans `src/modules/_shared/api/contracts.ts` (portée réelle : §2.1) | `pnpm gen:api:check` (échoue si le fichier généré a dérivé) + test « CA2 » |
| 3 | Routes préfixées `/api/v1/`, ressources au pluriel en kebab-case | `servers[0].url` du contrat ; règle **unique** `checkResourcePath()` dans `src/modules/_shared/api/path-rules.ts`, partagée par `assertRoutePath()` et le lint | test « CA3 » (contrat) + `gescom-middleware.contract.test.ts` (refus à la définition) |
| 4 | Tenant toujours résolu depuis le jeton, jamais un paramètre | `src/modules/_shared/application/tenant-resolution.ts` | test « CA4 » : `?tenant_id=` → 400 `api.tenant_not_addressable` ; `{tenantId}` en chemin → la route ne se déclare pas |
| 5 | Deux modes d'auth : Bearer JWT utilisateur et clé de service à scopes | `securitySchemes.bearerAuth` / `serviceKey` ; `resolvePrincipal()` + `assertScopes()` **fermé par défaut** (§3.1) | tests « CA5 » : route sans scope refusée à la définition, clé de service refusée en 403, `x-required-scopes` linté sur le contrat |
| 6 | Succès `{ data, meta }` ; erreur RFC 7807 `application/problem+json` avec `code` métier stable | `buildEnvelope()` et `renderProblem()` dans `gescom-middleware.ts` ; `problem.ts` | tests « CA6 » |
| 7 | Pagination par curseur `?page[size]&page[cursor]`, suivant dans `meta.next_cursor` | `src/modules/_shared/application/pagination.ts` | test « CA7 » |
| 8 | Tout POST créant une ressource honore `Idempotency-Key` | `idempotency.ts` + table `public.api_idempotency_keys` ; empreinte = méthode + chemin + **query** + corps | tests « CA8 », rejeu et réutilisation abusive compris |
| 9 | Tout PATCH protégé par `ETag`/`If-Match` ; conflit → 409 avec l'état courant | `concurrency.ts` ; `concurrencyGuarded` non désactivable sur un PATCH ; **`If-Match: *` refusé** (§3.2) | tests « CA9 » : 428 sans en-tête, 400 sur `*`, 409 sur ETag périmé, 200 + nouvel ETag sinon |
| 10 | Bus d'événements `outbox_events`, payload versionné, signature HMAC | `outbox.ts` + migration `20260901000100_gescom_outbox_events.sql` ; section `webhooks` du contrat | test « CA10 » (contrat) **et** `tests/sql/gescom-outbox-append-only.sql`, qui exerce réellement trigger et RLS via `pnpm test:storefront:sql` |
| 11 | Organisation modulaire cohérente avec le dépôt ; aucun composant React n'interroge la base | `src/modules/_shared/` (pas de `ui/`) | `pnpm test:architecture` → `gescom-api-socle-boundaries.test.ts` |
| 12 | Test de contrat par endpoint, CI bloquante | `tests/contract/` + `.github/workflows/architecture.yml` | `pnpm test:contract` |
| 13 | v1 additive uniquement ; changement cassant → `/api/v2` | §7 de ce document + en-tête du contrat | test « CA13 » |

---

## 2. Décisions techniques prises en E10.0

### 2.1 Outillage `gen:api` — contrat-first, `openapi-typescript`

**Retenu : `openapi-typescript` 7.13.0.** Le YAML est la source, les types TypeScript sont un artefact dérivé, régénéré par `pnpm gen:api` et **commité** (pour que `pnpm typecheck` et la CI n'aient pas à regénérer).

**Écarté : `zod-openapi`.** La compatibilité a été vérifiée, pas supposée : `zod-openapi@6.0.2` déclare `peerDependencies: { "zod": "^4.0.0" }`, et le dépôt est en `zod@4.4.3` — il **fonctionnerait**. Il est écarté pour une raison de direction, pas de version : `zod-openapi` va du code vers le contrat (code-first). Le YAML deviendrait un artefact de build, ce qui contredit frontalement le CA1 et l'attente du partenaire, qui veut un contrat lisible **avant** que le code existe.

Conséquence assumée : les schémas Zod du socle et le contrat décrivent la même chose à deux endroits. Deux garde-fous empêchent la dérive silencieuse.

**Ce que `CONTRACT_ALIGNMENT` garantit, et ce qu'il ne garantit pas.** Cette assertion de compilation (`src/modules/_shared/api/contracts.ts`) attrape la disparition ou le renommage d'un champ, un champ requis devenu incompatible, et surtout les **énumérations** — `EventName` est généré en union de littéraux, donc ajouter une valeur côté Zod sans l'ajouter au YAML ne compile plus.

Elle n'attrape **rien** de ce que le contrat exprime en `pattern`, `format`, `minimum` ou `maxLength`. `Money` et `Rate` sont générés en `string` des deux côtés : l'assertion y est **tautologique** (`string extends string`) et ne prouve rien sur le format. Un montant sérialisé en flottant passerait sans bruit.

La garantie de format est portée par le second garde-fou, à l'exécution : **`tests/contract/shared-components.contract.test.ts`** valide via Ajv 2020 les payloads réellement produits par le code contre le JSON Schema du contrat, dans les deux sens (ce que Zod produit est légal au contrat ; ce que le contrat déclare légal est accepté par Zod). C'est ce test-là qui tient le CA2 sur les formats.

### 2.2 Sort de `DomainEvent`

`src/kernel/events/index.ts` définissait `DomainEvent<Name, Payload>` sans aucun usage dans le dépôt.

**Décision : conservé et réutilisé comme socle, pas remplacé.** `OutboxEvent` en dérive par intersection (`src/modules/_shared/application/outbox.ts`). `DomainEvent` apportait déjà `id` / `name` / `occurredAt` / `tenantId` / `aggregateId` / `payload` ; il lui manquait les deux champs exigés par le CA10 : la **version du payload** et le **type d'agrégat**. Ils sont ajoutés côté module, pas dans le kernel — le kernel reste minimal (R4) et aucun code existant n'est touché.

### 2.3 Deux façades HTTP qui coexistent

`createApiV1Handler` (historique) et `createGescomApiHandler` (E10) tournent côte à côte. Elles partagent le routage (`compilePathTemplate`, exporté depuis `api-v1-handler.ts`) mais pas la forme de réponse :

| | Historique | E10 |
|---|---|---|
| Succès | payload nu | `{ data, meta }` |
| Erreur | `{ ..., requestId }` | `{ ..., request_id, code }` en `application/problem+json` |

Aligner l'historique casserait ses clients (dérogation R5 — voir §8).

**Comment elles coexistent concrètement** (décision du montage, E10.0) : elles sont servies par **la même edge function**, `supabase/functions/magrit-api/index.ts`, sur le même préfixe `/api/v1`. `createMagritApiApplication()` compose les deux et place devant elles `createApiFacadeRouter()`, qui aiguille **d'après le chemin** :

- le chemin correspond à une route de `gescomRoutes()` → façade E10 ;
- sinon → façade historique, inchangée.

*Pourquoi pas une edge function séparée* : il aurait fallu un second déploiement, une seconde configuration CORS et surtout une règle de routage côté hébergeur pour découper `/api/v1` entre deux fonctions — de la configuration de déploiement que ce sprint ne peut pas livrer. Le front (`FetchApiClient`) pointe déjà sur `/api/v1/...` sans savoir qui répond : router en interne le laisse inchangé.

*Pourquoi aiguiller sur le chemin seul, et pas sur le couple chemin + méthode* : un `DELETE /api/v1/customers` non déclaré retomberait sinon sur la façade historique, qui répondrait 404 dans **son** format (`requestId` camelCase) à un client qui attend `request_id`. Un chemin appartient à une façade, avec toutes ses méthodes.

*Collisions* : deux façades qui partagent un espace d'URL peuvent se recouvrir, et un recouvrement rendrait une route historique injoignable **en silence**. Le recouvrement par paramètre est traité : `/customers/{id}` capterait `/customers/export`.

`assertNoFacadeCollision()` s'exécute à **deux moments**, et il faut savoir lequel fait quoi :

1. **Au chargement du module** `src/server/api/legacy-routes.ts`, sur `GESCOM_ROUTES` × `LEGACY_ROUTE_DEFINITIONS` — deux listes de **définitions**, construites sans service réel, donc disponibles avant toute requête. L'edge function importe ce module : une collision fait échouer le démarrage à froid, franchement, et la CI la voit au premier test qui importe le module.
2. **À chaque composition**, dans `createMagritApiApplication()`. Ce second passage est une conséquence du fait que les services sont liés au client Supabase de la requête : la composition ne peut pas être hissée au chargement du module sans changer la façon dont les clients sont injectés. Il ne remplace pas le premier — il le double.

Autrement dit : la garantie « ça ne démarre pas » est portée par (1), pas par (2). Avant l'extraction de `legacy-routes.ts`, seul (2) existait, et une collision aurait levé une erreur **à chaque requête, en continu**, au lieu d'un échec de boot propre.

### 2.4 Table `api_idempotency_keys`

Le CA8 exige que l'`Idempotency-Key` soit **honorée**, ce qui suppose un stockage qui survive au redémarrage du process : c'est justement après un incident réseau que le client retente. Le socle définit le port `IdempotencyStore` et une implémentation en mémoire pour les tests ; la table `public.api_idempotency_keys` (migration `20260901000200`) est son support durable. L'adaptateur Supabase du port sera écrit par la première story E10.x qui expose une création.

---

## 3. Règles transverses opposables

### 3.1 Scopes de clé de service — fermé par défaut

Une opération joignable par une clé de service **doit** déclarer ses scopes, en code (`requiredScopes`) comme au contrat (`x-required-scopes`). La règle est fermée par défaut, dans les deux sens :

- `defineGescomRoute()` **refuse à la définition** toute route dont `authentication` vaut `'any'` (le défaut) ou `'service'` sans `requiredScopes` non vide ;
- `assertScopes()` **refuse en 403** une clé de service qui atteindrait malgré tout une opération sans scope déclaré ;
- `lintRequiredScopes()` refuse un contrat où une opération joignable par `serviceKey` n'annonce pas ses `x-required-scopes`, ou en annonce un absent de `x-magrit-scopes` ;
- `lintRoutesAgainstContract()` refuse un écart entre les scopes du code et ceux du contrat.

La seule dispense est de restreindre explicitement l'opération aux jetons utilisateur : `authentication: 'user'`. Leurs droits viennent des rôles du tenant, vérifiés par la RLS et le service métier, pas de scopes.

Laisser passer une liste vide reviendrait à transformer chaque oubli de déclaration en ouverture silencieuse de l'opération à **toutes** les clés du tenant — Studio pourrait écrire là où il n'a que la lecture.

### 3.2 `If-Match: *` est refusé

Contrairement à la sémantique RFC 7232, où `*` signifie « pourvu que la ressource existe », la façade le refuse en **400 `api.if_match_invalid`**. L'accepter reviendrait à laisser un appelant désactiver le contrôle de concurrence à sa guise : deux commerciaux éditant la même fiche s'écraseraient en silence, ce que le CA9 interdit. Le `pattern` du paramètre `IfMatch` au contrat n'admet qu'un ETag, faible ou fort.

### 3.3 Rejeu d'idempotence

Une requête est identifiée par **méthode + chemin + query + corps** — deux POST au même chemin avec des query différentes ne sont pas la même requête. Sur un rejeu :

- la réponse mémorisée est rendue telle quelle, `data` compris ;
- `meta.request_id` est **recalé sur la requête courante**, pour que la promesse « `meta.request_id` == en-tête `X-Request-Id` » reste vraie sans exception ;
- l'en-tête `Idempotency-Replayed: true` signale le rejeu, ce que le seul statut 201 ne dit pas.

### 3.4 Sélection de l'espace de travail — `X-Magrit-Tenant`

> ✅ **Amendement au CA4 — RATIFIÉ le 2026-09-01 (décision Arnaud).** Le CA4 avait été accepté tel quel par la revue du Lot 0. Le montage a montré qu'il était **inapplicable en l'état** : aucun claim du JWT ne porte le tenant Magrit. La lecture décrite ci-dessous est la règle en vigueur ; elle ne se rediscute plus story par story.

**Le problème.** Le CA4 impose que le tenant vienne du jeton. Or un JWT Supabase ne porte **aucun** tenant Magrit, et un compte appartient régulièrement à plusieurs espaces — un tenant parent et ses sous-tenants, une agence et ses clients. Le front lui-même résout l'espace courant depuis l'URL `/t/:slug` (voir `TenantContext`), pas depuis la session. Le port `PrincipalVerifier` n'avait donc **aucune implémentation possible** : E10.4 ne pouvait pas savoir de quel espace lister les clients.

Deviner « le » tenant d'un compte multi-espaces reviendrait à lui montrer le référentiel client d'un autre espace que celui qu'il consulte. C'est un défaut de confidentialité, pas une approximation d'ergonomie.

**La règle : le jeton autorise, l'en-tête sélectionne.**

| `X-Magrit-Tenant` | Espaces accessibles | Résultat |
|---|---|---|
| absent | exactement 1 | cet espace |
| absent | plusieurs | **400** `identity.tenant_selection_required` — l'API ne devine pas |
| absent | aucun | **403** `identity.tenant_not_resolved` |
| présent | l'espace en fait partie | cet espace |
| présent | l'espace n'en fait pas partie | **403** `identity.tenant_not_resolved`, réponse identique à celle d'un espace inexistant |

L'en-tête ne peut **jamais élargir** les droits : il choisit parmi ce que le jeton autorise déjà, et l'habilitation réelle reste tenue par la RLS. C'est le sens défendable du CA4 — interdire qu'un paramètre fasse **autorité**, ce que faisait `/tenants/{tenantId}/...`. Un en-tête n'est d'ailleurs ni un paramètre de chemin ni un paramètre de requête, les deux formes que le CA4 nomme.

`SupabaseApiPrincipalVerifier` interroge `current_user_tenant_ids()`, **la même fonction** que celle utilisée par les policies RLS : la façade et la base ne peuvent pas être en désaccord sur ce qui est accessible.

Côté front, `WorkspaceModuleUiBridge` attache l'en-tête depuis le tenant courant. Les clients de module ne transportent pas cette notion — `CustomersApiClient` ne connaît que sa ressource.

Une **clé de service** ignore cet en-tête : elle est émise **pour** un espace, qu'elle porte déjà.

### 3.5 Droits métier d'un jeton utilisateur — `x-required-capabilities` (E10.11)

> Posé par **E10.11**. Symétrique de §3.1 (scopes de clé de service) sur l'autre axe d'authentification.

Une clé de service porte ses droits **avec elle** (scopes). Un jeton utilisateur n'en porte **aucun** : il prouve une identité, et ses droits dans l'espace résolu sont lus **en base à chaque appel**. Les graver dans le JWT ferait survivre un droit à son retrait pendant toute la validité du jeton.

**Le mécanisme n'appartient pas à E10 et n'est pas dupliqué par lui.** Il existe depuis S-ORDER-ROLES :

| Élément | Où |
|---|---|
| Définitions de rôles du tenant, colonne jsonb `capabilities` | `public.tenant_role_definitions` |
| Affectations à un utilisateur | `public.tenant_role_assignments` |
| Évaluation d'un droit | `public.user_has_capability(p_tenant_id, p_capability)` |
| Administration (créer un rôle, l'affecter, lire le profil) | **façade historique** — `/api/v1/tenants/{tenantId}/roles*`, `/access-profile`, `/capabilities/{capability}` |
| Résolution côté UI | `resolveCapability()` — `src/modules/roles/ui/runtime/accessProfile.helpers.ts` |

**Nommage** : le nom canonique d'un droit est celui écrit en base, forme `can_*` en snake_case (`can_manage_roles`, `can_manage_shops`, `can_manage_pricing`). C'est ce nom, et lui seul, qui apparaît dans le contrat.

**Règle de dérivation, portée par `user_has_capability` et non par la façade** : l'appartenance `admin` d'un espace confère **tous** les droits métier (`20260814000200_admin_unique.sql`, qui a par ailleurs fusionné `owner` dans `admin`). Conséquence directe : remplacer une garde « rôle `admin` » par un droit dédié n'enlève l'accès à **personne** — la population autorisée est **identique**, seule change la façon dont le contrat la nomme. C'est ce qui rend ce type de substitution **additive** au sens du CA13 (§7).

**Ce que la dérivation ne dit PAS** : qu'un membre ordinaire peut recevoir le droit. La seconde branche de `user_has_capability` (rôle affecté portant la capability) est bien réelle en SQL, mais elle est **verrouillée en amont** pour les membres Magrit par le trigger UM1 `restrict_magrit_assignments_to_options` (`20260824000200_um1_admin_shop_guards.sql`) : toute affectation à un membre dont l'`access_scope` vaut `magrit_full` (valeur par défaut, `20260505000200_e9_user_permissions.sql`) est rejetée (`magrit_option_required`, SQLSTATE 23514) si la définition visée ne porte pas l'un des deux `system_key` `option_shops` / `option_orders`. Ce n'est donc pas « impossible dans l'absolu » — c'est impossible pour tout rôle qui n'est pas l'une de ces deux options produit, et aucune ne porte de capability tarifaire. **Conséquence opposable : tant que ce verrou tient, un droit métier E10 est détenu par les seuls `admin` (et le super-admin de plateforme). Aucun contrat ne doit promettre une délégation à un membre ordinaire sans avoir vérifié qu'un chemin d'affectation existe réellement en base.** Rouvrir la délégation (option système dédiée, élargissement du trigger) est une décision produit à part entière — écartée par Arnaud le 04/09 au profit du maintien de « admin unique » (chantier UM, 14/08).

**Ce que le contrat déclare :**

- `components.securitySchemes.bearerAuth.x-magrit-capabilities` — le vocabulaire, une entrée par droit, avec sa définition ; symétrique de `x-magrit-scopes` ;
- `x-required-capabilities: [...]` au niveau d'une **opération** — le droit qu'elle exige ; symétrique de `x-required-scopes` ;
- réponse partagée `ForbiddenCapability` (403) sur ces opérations.

**Ce que le contrat ne déclare pas, et ne déclarera pas** : aucune opération pour **accorder** un droit. L'administration des rôles est servie par la façade historique ; ouvrir un doublon dans la façade E10 créerait deux vérités sur la même table. Corollaire à ne pas franchir dans l'autre sens : puisque l'octroi est hors contrat, le contrat ne doit pas non plus **promettre** qu'un octroi aboutira. Il décrit le droit exigé par une opération, jamais le parcours pour l'obtenir.

**Règles opposables :**

1. Une opération sans `x-required-capabilities` n'exige que l'**appartenance** à l'espace. L'absence de déclaration n'est pas une ouverture, c'est le régime normal.
2. Une opération gardée par un droit métier est **réservée aux jetons utilisateur** (`security: [bearerAuth]`). Une clé de service n'a pas d'identité à qui un rôle serait affecté ; l'y rendre joignable serait un contresens.
3. Le code d'erreur reste **`identity.role_required`**, déjà publié en v1. Il signifie « habilitation utilisateur insuffisante », ce qui reste exact ; seul change **ce qui établit** l'habilitation. Le renommer en `identity.capability_required` serait cassant (§7) pour un gain purement lexical.
4. **La garde doit exister en base**, pas seulement dans la façade. Une garde applicative seule est contournée par un appel PostgREST direct. Policy RLS sur `user_has_capability`, testée par un cas SQL — pas seulement déclarée (`.claude/rules/db.md`).
5. **Une garde d'UI n'est pas une garde d'autorisation.** `requiredCapabilities` sur une route de surface est de l'ergonomie (ne pas proposer un écran inutilisable) ; elle ne protège rien et ne dispense d'aucune vérification serveur (`.claude/rules/frontend.md`).

---

## 4. Forme des réponses

### Succès

```json
{
  "data": { "id": "b1c2d3e4-5f60-4a7b-8c9d-0e1f2a3b4c5d", "name": "Fidélité" },
  "meta": { "request_id": "0f8c...", "next_cursor": null, "page_size": 50 }
}
```

`meta.request_id` est toujours présent et vaut l'en-tête `X-Request-Id`.

### Erreur — RFC 7807, `Content-Type: application/problem+json`

```json
{
  "type": "about:blank",
  "title": "Conflit de version",
  "status": 409,
  "code": "api.resource_conflict",
  "request_id": "0f8c...",
  "detail": "La ressource a changé depuis la lecture.",
  "current_state": { "id": "...", "name": "Fidélité" }
}
```

`code` est **contractuel** : un client a le droit de brancher son comportement dessus. Forme `domaine.raison` en snake_case. Un code publié n'est jamais renommé en v1 — on en ajoute, on n'en retire pas.

Codes transverses du socle : voir `SHARED_PROBLEM_CODES` dans `src/modules/_shared/application/problem.ts`. Chaque module E10.x ajoute les siens sous son propre domaine (`quote.*`, `order.*`, `customer.*`, `price_rule.*`).

---

## 5. Typage — règles opposables

| Notion | Base | JSON | Exemple |
|---|---|---|---|
| Montant | `numeric(12,2)` | **chaîne** décimale, 2 décimales | `"1234.50"` |
| Taux | `numeric(6,4)` | **chaîne**, 4 décimales | `"0.5000"` = 50 % |
| Date / instant | `timestamptz` | ISO 8601 **UTC**, suffixe `Z` | `"2026-09-01T08:30:00.000Z"` |
| Date seule | `date` | `YYYY-MM-DD` | `"2026-09-01"` |
| Identifiant technique | `uuid` (v4) | UUID v4 | `"b1c2d3e4-5f60-4a7b-8c9d-0e1f2a3b4c5d"` |
| Numéro métier | colonne `text` | attribut, **jamais une clé** | `"DEV-2026-00042"` |
| Énumération | `text` + `check` | **snake_case** | `"margin_pct"` |

**Aucun montant ni taux en flottant JSON.** Un flottant perd des centimes sur les arrondis de TVA et de remise, et l'erreur ne se voit qu'à la facture.

### Piège découvert en production (2026-09-02) — normaliser TOUT timestamp lu d'une ligne Postgres

**Ne jamais restituer `row.created_at`/`row.updated_at` (ou tout champ `timestamptz`) tel quel depuis une ligne Supabase.** PostgREST sérialise systématiquement avec un décalage explicite (`2026-09-02T06:54:39.688293+00:00`), **jamais** avec le suffixe `Z` — y compris pour une valeur stockée en UTC. `timestampSchema` (regex stricte, exige le `Z` littéral) rejette cette forme : toute route qui renvoie une valeur de date lue brute échoue en 500 `api.internal_error` à la validation de réponse (`gescom-middleware.ts`), **avec des données parfaitement valides**, et **après que l'écriture métier a déjà réussi** — le client voit une erreur alors que la ressource existe en base (créée en double au prochain essai si le SIRET/libellé n'a pas de contrainte d'unicité qui l'en empêche).

Ce défaut est resté invisible pendant tout le Lot 1 et le Lot 2 parce que les fakes des suites de contrat construisent leurs dates via `new Date().toISOString()`, qui produit déjà `Z` — jamais exercé contre PostgREST. Ni `pnpm test`, ni `pnpm test:contract`, ni `pnpm test:architecture` ne pouvaient le détecter. Découvert en production le 2026-09-02 suite à un signalement direct (création de client systématiquement en "erreur interne", personne physique et morale), diagnostiqué par lecture des logs de la edge function déployée.

**Règle désormais opposable** : tout adaptateur qui mappe une ligne Postgres vers un DTO doit passer chaque champ `timestamptz` par `toIsoTimestamp()` (requis) ou `toIsoTimestampOrNull()` (nullable), exportés par `src/modules/_shared/application/index.ts`. Corrigé sur les 4 adaptateurs E10 existants (`customers`, `projects`, `project-tags`, `commercial-quotes`) ; toute story future qui ajoute un adaptateur doit faire de même — sans quoi le même défaut réapparaît, silencieusement en test, bruyamment en production.

---

## 6. Écrire un endpoint E10.x — la séquence

1. **Décrire l'opération dans `openapi/magrit-core.v1.yaml`** (seul l'agent `architecte` y touche). Réutiliser les composants partagés : `Problem`, `Meta`, `Money`, `Rate`, `Audit`, `PageSize`, `PageCursor`, `IdempotencyKey`, `IfMatch`, et les réponses d'erreur mutualisées.
2. **`pnpm gen:api`**, committer le fichier généré.
3. **Contrats Zod** dans `src/modules/<domaine>/api/contracts.ts`, client HTTP dans `api/client.ts`.
4. **Interface repository + service** dans `src/modules/<domaine>/application/`.
5. **Implémentation Supabase** dans `src/adapters/supabase/<domaine>-repository.ts`.
6. **Routes** dans `src/server/api/<domaine>-routes.ts`, via `defineGescomRoute()`.
7. **Test de contrat** dans `tests/contract/<domaine>.contract.test.ts`, avec `checkResponseAgainstContract()`.

### Convention de dossiers — précision opposable

E10.0 mentionnait littéralement une structure `routes/service/repository/dto/events/__tests__`. **Cette formulation est amendée.** Le socle et les modules E10.x suivent la convention **réellement en place** dans le dépôt, celle des 10 modules existants (`commercial`, `quotes`, `orders`, `shop-customers`, `catalog`, `members`, `roles`, `invitations`, `shops`, `libraries`) :

```
src/modules/<domaine>/
  api/contracts.ts          contrats Zod partagés client/serveur
  api/client.ts             client HTTP typé
  application/<d>-repository.ts   interface (port)
  application/<d>-service.ts      logique applicative
src/adapters/supabase/<domaine>-repository.ts   implémentation
src/server/api/<domaine>-routes.ts              routes HTTP
```

Introduire la structure littérale de l'énoncé aurait créé une onzième convention dans un dépôt qui en a déjà une, appliquée partout. Le socle transverse vit dans `src/modules/_shared/`, qui suit le même pattern et n'a **pas** de dossier `ui/` : il ne publie aucune UX.

---

## 7. Versionnement — v1 additive uniquement

**Autorisé en v1** (additif) :

- ajouter une opération, un chemin, un webhook ;
- ajouter un champ **optionnel** à une réponse ;
- ajouter une valeur à une énumération **de réponse** ;
- ajouter un paramètre optionnel, un code d'erreur, un scope de clé de service ;
- élargir une contrainte (augmenter un `maxLength`, relâcher un `pattern`).

**Interdit en v1** — exige `/api/v2` :

- retirer ou renommer un champ, un chemin, une opération, un `operationId` ;
- rendre obligatoire un champ jusque-là optionnel ;
- changer le type ou le format d'un champ (`number` → `string`, décimales) ;
- retirer une valeur d'énumération, ou en ajouter une à une énumération **de requête** que le serveur refuserait ;
- changer le statut HTTP d'une réponse existante, ou renommer un `code` métier ;
- restreindre une contrainte (baisser un `maxLength`, durcir un `pattern`).

`/api/v1` n'est **jamais** muté rétroactivement. Une v2 se monte à côté ; v1 reste servie jusqu'à extinction annoncée de ses clients.

---

## 8. Dérogations R5 en cours

| Dérogation | Portée | Chemin de mise en conformité |
|---|---|---|
| `docs/architecture/api/openapi.yaml` et la façade `createApiV1Handler` restent en `requestId` (camelCase) et payload nu, hors enveloppe `{ data, meta }` | Endpoints historiques uniquement (`/health`, `/diagnostics/*`, modules pré-E10) | Migration endpoint par endpoint vers l'enveloppe E10 lors de leur prochaine évolution fonctionnelle, ou bascule groupée en `/api/v2`. Aucun **nouvel** endpoint n'est admis sur l'ancienne forme. |
| Le port `IdempotencyStore` n'a que son implémentation en mémoire | Socle E10.0, qui n'expose aucune création | Adaptateur `src/adapters/supabase/api-idempotency-store.ts` sur la table `api_idempotency_keys`, à écrire par la première story E10.x qui expose un POST de création. |
| Le dispatcher qui relaie `outbox_events` n'est pas écrit | Socle E10.0, qui n'émet aucun événement | À livrer avec la première story qui publie un événement. Le mécanisme d'écriture, la signature et l'enveloppe sont déjà en place et testés. |

**Aucune dérogation n'est admise sur du code nouveau** (R5). Les trois lignes ci-dessus sont des périmètres non encore implémentés, pas des écarts dans ce qui a été livré.

### 8.1 Dette de vérification acceptée à la clôture du Lot 0

Ces points sont des **trous de couverture**, pas des défauts de comportement : le code fait ce qu'il doit, mais rien ne le prouverait automatiquement s'il cessait de le faire. Acceptés à la clôture du Lot 0 (revue qa-review), à résorber par les stories indiquées.

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **B3** | `tests/sql/gescom-outbox-append-only.sql` n'a **jamais été exécuté** — le poste de rédaction n'avait ni Docker ni `psql`. Le fichier est écrit et enregistré dans `SQL_CASES`, mais son verdict est inconnu. | Un cas SQL jamais lancé peut échouer sur une broutille (colonne manquante, seed absent) et donner l'illusion d'une garantie. | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé, **avant** la première story E10.x qui émet un événement. Corriger le cas s'il échoue. |
| **R1** | Le cas SQL accorde `grant select ... to authenticated` pour exercer la policy, puis le retire. Il ne teste donc **pas** l'état réel de production, où `authenticated` n'a aucun privilège : la policy est vérifiée, la fermeture par `revoke` l'est séparément, mais jamais les deux ensemble dans l'ordre réel. | Si un jour un `grant` est accordé par erreur, le test le détecte (dernière assertion). Mais si la policy était supprimée **et** le grant maintenu, l'ordre actuel des assertions ne le verrait pas. | Ajouter un scénario qui, sans aucun grant, vérifie que `authenticated` reçoit bien `42501` sur `select`. À faire avec le premier passage réel du cas (B3). |
| **R2** | Aucun test ne couvre `public.api_idempotency_keys` : ni ses contraintes (`unique (tenant_id, idempotency_key)`, forme de l'empreinte, cohérence `completed`), ni sa fermeture aux rôles client. | La table est le support durable du CA8. Une contrainte qui ne tient pas ferait silencieusement dériver l'idempotence vers du best-effort. | Cas SQL `tests/sql/gescom-idempotency-keys.sql`, à écrire avec l'adaptateur Supabase du port `IdempotencyStore` (première story E10.x exposant un POST). |
| **R3** | Le garde B2 tient sur **deux gonds** non redondants : le test d'architecture vérifie que tout fichier appelant `defineGescomRoute(` est cité par `gescom-routes.ts`, et le test de contrat vérifie que les routes du registre existent au contrat. Une route déclarée **hors** de `src/server/api/` (dans un module, par exemple) échapperait au premier gond, donc au second. | Le CA1 reposerait alors sur la discipline, ce que ce garde était censé remplacer. | Élargir le balayage du test d'architecture à `src/modules/**` et `src/adapters/**`, ou déplacer l'assertion dans `createGescomApiHandler` (refus au démarrage d'une route absente d'un manifeste d'operationId généré depuis le contrat). À faire à la première story qui publie un endpoint. |

### 8.2 Dette introduite par le montage des façades

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **M1** | L'edge function `supabase/functions/magrit-api/index.ts` n'est **ni typecheckée ni testée** : elle n'est dans aucun `tsconfig` (`include` ne couvre pas `supabase/`) et son exécution demande Deno + un déploiement Supabase. | Une erreur de câblage dans ce fichier n'est vue qu'au déploiement. C'est le seul maillon du montage qu'aucune commande locale ne couvre. | La composition a été **sortie du fichier** vers `createMagritApiApplication()` (dans `src/`, typechecké et testé) pour réduire au minimum ce qui reste non couvert : il n'y subsiste que l'instanciation des adaptateurs. Couverture réelle possible via `deno check` en CI. |
| **M2** | L'écriture dans `outbox_events` n'est **pas transactionnelle** avec l'écriture métier : les deux passent par PostgREST en deux appels HTTP. Le pattern outbox suppose l'atomicité. | Un événement peut se perdre si l'écriture métier réussit et l'ajout à l'outbox échoue. `bestEffortOutbox` journalise sans faire échouer la requête — échouer dirait au client que son opération a échoué alors qu'elle a réussi, et il rejouerait en créant un doublon. | Déplacer l'écriture métier **et** l'ajout à l'outbox dans une même fonction `api_*` `security definer`. À trancher avec la première story qui rend un événement critique (facturation, Studio). |
| **M3** | Le store d'idempotence monté est `InMemoryIdempotencyStore` : il ne survit pas au recyclage de l'isolat Deno. | La garantie du CA8 ne couvre qu'une fenêtre courte. Deux tentatives séparées par un recyclage créeraient un doublon. | Adaptateur Supabase sur `api_idempotency_keys` — déjà tracé en §8.1 (R2) et §8 (dérogations R5). Le montage rend cette dette **active** : elle n'était que théorique tant qu'aucun POST n'était joignable. |

### 8.3 Dette signalée sur E10.4 (module Clients), acceptée

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **m5** | Le trigger qui réinitialise `siret_verified` quand le SIRET change n'a **aucun test SQL réel**. Il n'est couvert que par un test de contrat contre un faux repository qui **réimplémente** le comportement du trigger. | Un test qui rejoue en TypeScript ce que fait le trigger prouve que le faux est cohérent avec lui-même, pas que le trigger existe ni qu'il fonctionne. Le supprimer ne casserait aucun test. | Ajouter le scénario au cas SQL `tests/sql/gescom-e10-4-customers.sql` : insérer un client SIRET vérifié, changer le SIRET, vérifier que `siret_verified` retombe à `false`. À faire au premier passage réel des cas SQL (§8.1 B3, Docker requis). |

**Corrigé plutôt que tracé** : `markSiretVerified` filtrait sur `tenant_id` + `id` sans le SIRET (m4). Un appel INSEE dure ; un `PATCH` concurrent changeant le SIRET pendant la vérification faisait apposer « vérifié » sur un numéro que personne n'avait contrôlé — un faux positif à valeur commerciale, pas une approximation. Le SIRET vérifié est désormais une **condition d'écriture** : zéro ligne touchée → `CustomerNotFoundError`, la vérification est perdue et relançable, ce qui est le bon comportement. Un test de contrat joue la course (crochet d'interleaving sur le délai INSEE) et vérifie que le résultat est perdu, pas appliqué.

> **Piège à connaître : un faux qui diverge de son port sans que rien ne le dise.**
> Ce correctif a d'abord été livré incomplet. Le port exigeait la nouvelle condition, l'adaptateur Supabase l'appliquait, mais le faux repository des tests de contrat avait gardé l'ancienne signature — et **passait le typecheck**, parce que TypeScript autorise la bivariance sur les paramètres de méthode. Une méthode déclarée avec moins de paramètres reste assignable.
>
> Conséquence : la suite de contrat attestait le comportement **inverse** de la production, et retirer la garde de l'adaptateur n'aurait fait échouer aucun test.
>
> À retenir pour toute évolution de port : `tsc` ne garantit pas qu'un faux honore le contrat qu'il implémente. Seul un test **comportemental** sur le cas que la garde protège le prouve. Écrire ce test fait partie du changement de port, pas d'un lot de suivi.

**Ce qui a été corrigé au montage et n'est donc pas de la dette** : `Access-Control-Allow-Headers` ne listait ni `idempotency-key` ni `if-match`, et `Access-Control-Expose-Headers` était absent. Le préflight aurait rejeté toute création et toute modification E10.4, et `response.headers.get('etag')` aurait rendu `null` dans le navigateur — le flux `If-Match` était inutilisable depuis le front, indépendamment du montage.

**Ce qui a été corrigé et n'est donc pas de la dette** : `Timestamp` déclarait `format: date-time` sans `pattern`, ce qui laissait le contrat accepter `2026-09-01T08:30:00+02:00` que Zod refusait. Corrigé à la clôture du Lot 0 — c'était la dernière fenêtre où durcir ce schéma restait gratuit, la règle « v1 additive » (§7) l'interdisant dès la publication du premier endpoint. La divergence est désormais impossible à réintroduire sans être vue : le test de **parité** de `shared-components.contract.test.ts` confronte, échantillon par échantillon, le verdict Zod et le verdict du contrat sur tous les scalaires.

### 8.4 Dette signalée sur E10.5 (dissociation comptes Magrit / comptes clients boutique), acceptée

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **n1** | `tests/sql/gescom-e10-5-shop-customer-link.sql` (unicité par boutique, cohérence inter-tenant, exclusivité `tenant_members` ⟺ `shop_customer_accounts`, `current_user_is_shop_customer()`) n'a **jamais été exécuté** — même contrainte que B3 (§8.1), Docker indisponible au poste de rédaction. | Une contrainte SQL écrite mais jamais lancée peut échouer sur une broutille et donner l'illusion d'une garantie prouvée. | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé. Le fichier est déjà enregistré dans `SQL_CASES` (`scripts/test-storefront-sql.sh`). |
| **n2** | `SupabaseApiPrincipalVerifier.isShopCustomer()` **échoue ouvert** (`return false`) si l'appel RPC `current_user_is_shop_customer()` erre, au lieu d'échouer fermé (503) comme `accessibleTenantIds()`. | Une erreur transitoire sur cette primitive laisserait passer un compte client boutique jusqu'au refus générique (0 espace accessible → 403 `identity.tenant_not_resolved`), pas jusqu'au 403 `auth.scope_forbidden` nommé. Ce n'est **pas** un trou de sécurité : la RLS (fermée par défaut sur `shop_customer_accounts`, membership `tenant_members` inexistante par construction, CA5) reste la barrière réelle — ce garde n'est qu'un diagnostic plus précis. | Si l'observabilité montre des erreurs récurrentes sur cette RPC en production, aligner sur `accessibleTenantIds()` (503) plutôt que fail-open. Décision volontairement différée pour ne pas faire d'une primitive de confort une nouvelle cause de panne totale de la façade. |
| **n3** | La fiche client (CustomerDetailPage) n'affiche qu'**un seul** badge d'accès boutique par interlocuteur (`shop_accesses[0]`), même si le modèle autorise un accès par boutique. | Un interlocuteur avec des accès dans plusieurs boutiques du même tenant ne verrait que le premier dans l'UI (l'API, elle, restitue bien le tableau complet — `GET .../contacts` n'est pas concerné). | Étendre l'UI à une liste de badges quand un tenant multi-boutiques l'exige réellement ; pas de donnée inventée en attendant, juste une simplification d'affichage assumée (story hors périmètre : « aucune UI de gestion de compte boutique complète »). |

### 8.5 Dette signalée sur E10.1 (Espace Projets), acceptée

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **o1** | `tests/sql/gescom-e10-1-projects.sql` (contrainte NOT NULL sur `customer_id`, isolation par tenant via `using` ET `with check` sur `projects` ET `project_items` — B3 qa-review inclus, cf. scénario 5 —, persistance d'un élément après archivage) n'a **jamais été exécuté** — même contrainte que B3/n1 (§8.1/§8.4), Docker absent (`docker: command not found`) sur le poste de rédaction. | Un cas SQL écrit mais jamais lancé peut échouer sur une broutille et donner l'illusion d'une garantie prouvée. | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé. Le fichier est déjà enregistré dans `SQL_CASES` (`scripts/test-storefront-sql.sh`). |
| **o2** | Le déploiement de la migration `20260901000500_gescom_e10_1_projects.sql` sur le projet Supabase partagé (comme E10.4/E10.5 avant elle) n'a pas pu être effectué dans cette session : ni le CLI `supabase` (PAT absent), ni `psql`/`DATABASE_URL` direct ne sont disponibles. Vérifié : `public.projects` n'existe pas encore côté remote (sondé via `SUPABASE_SERVICE_ROLE_KEY` de `.env.test`). | L'endpoint fonctionne en mémoire (tests de contrat) et compile (`pnpm typecheck`/`pnpm build`), mais n'a pas encore de preuve d'exécution réelle contre Postgres, ni de disponibilité pour Studio. | Régénérer un PAT Supabase (cf. CLAUDE.md) et lancer `supabase db push` (ou équivalent) sur le projet `ightkxebexuzfjdbpsdg`, comme cela a été fait pour E10.4/E10.5. |
| **C1** | `DELETE /projects/{id}/items/{itemId}` sur un `itemId` inexistant (ou n'appartenant pas au projet) rend 200 `{ removed: true }` au lieu de 404 : `SupabaseProjectsRepository.removeItem()` ne vérifie pas le nombre de lignes réellement supprimées, seulement que le PROJET existe. | Un appelant (Studio compris) ne peut pas distinguer « élément retiré » de « élément déjà absent » ; ce n'est pas une fuite inter-tenant (le projet, lui, est bien vérifié), juste une réponse optimiste. | Lire le résultat du `.delete().select()` (ou un `count`) dans l'adaptateur ; si 0 ligne affectée, lever `ProjectNotFoundError` côté service pour cet item. À faire avec la prochaine évolution de cette route. |
| **C2** | `?status=<valeur hors enum>` (ex. `?status=bogus`) est silencieusement ignoré : `projectStatusSchema.safeParse` échoue et la route retombe sur « aucun filtre » plutôt que de rejeter la requête. | Un client qui se trompe de valeur croit filtrer et reçoit en fait la liste complète, sans avertissement. | Rejeter en 422 `api.validation_failed` (même traitement que `?status=` au contrat, qui déclare l'enum `ProjectStatus`) plutôt que d'ignorer silencieusement. À trancher avec une story qui touche à nouveau `listProjects`. |
| **C5** | Le faux repository de test (`tests/contract/_fakes/projects-repository.fake.ts`) calcule `position` d'un nouvel élément comme `siblings.length`, alors que l'adaptateur réel (`projects-repository.ts`) utilise `max(position) + 1`. Les deux divergent dès qu'un élément intermédiaire a été retiré (le faux « comble » le vide, l'adaptateur réel non). | Un test qui ajouterait un élément après un retrait verrait une position différente en mémoire et en réel, sans qu'aucun test actuel ne l'exerce (B2 a corrigé la pagination, pas ce point précis). | Ajouter un test de contrat qui retire un élément puis en ajoute un autre, et vérifier `position` — si le comportement contractuel de `position` après retrait doit être stabilisé, le trancher au préalable (assemblage contigu vs `max+1`), sinon aligner le faux sur `max(position)+1`. |
| **C6** | Suppression physique d'un projet possible via un appel direct PostgREST malgré CA6 (« jamais supprimé physiquement ») : les policies `projects_write`/`project_items_write` sont posées `for all`, sans distinguer `DELETE` des autres opérations — aucune route de l'API n'expose de `DELETE /projects/{id}`, mais la RLS seule ne l'interdit pas à un appel direct à la table. | Précédent déjà accepté à l'identique sur `customers` en E10.4 (même modélisation `for all`) : pas la peine de retraiter cette table isolément. | À arbitrer une fois pour toutes les tables du module Gestion commerciale (`customers`, `projects`, et les suivantes) quand l'orchestrateur en décidera — probablement une policy `for delete using (false)` explicite, ou un `revoke delete` ciblé. |
| **C7** | `ProjectsPage` (liste des projets) n'expose aucune pagination UI : elle affiche la première page (`page[size]` par défaut, 50) sans bouton « page suivante » ni indication qu'il pourrait y avoir plus de projets. | Un tenant avec plus de 50 projets actifs ne verrait silencieusement que les 50 plus récents, sans être informé qu'une partie de ses projets est masquée. | Ajouter un contrôle de pagination (bouton « Charger plus » consommant `meta.next_cursor`, ou indicateur explicite) à `ProjectsPage.tsx` lors d'une prochaine itération UI du module. |

### 8.6 Dette signalée sur E10.3 (Création d'un devis depuis un projet), acceptée

**Dépendance en attente — E10.21 (PricingEngine).** Les Dev Notes originales de la story renvoyaient à E10.8 pour les colonnes de prix des lignes de devis ; E10.8 a été **gelée** au WM du 01/09 (spécification seule, aucun code). Le Contrat API mis à jour (plus récent) impose le format `PricedLine` d'**E10.21** (interface PricingEngine), qui n'est pas encore livrée. `commercial_quote_lines` porte donc DÈS MAINTENANT les cinq colonnes du format cible (`production_price`, `public_price`, `customer_price`, `applied_margin_rate`, `applied_rule_id`, `breakdown`), pour éviter une migration cassante à l'arrivée d'E10.21, mais **seul `production_price` est renseigné** par cette story — repris tel quel du chiffrage source (`project_items.quote_payload.amounts.clariprint_price_ht`, sinon `.amounts.price`, sinon `0`), jamais recalculé. Les quatre autres colonnes restent `NULL`, `breakdown` reste `'[]'`. Aucun calcul de marge/prix public/prix client n'a été introduit hors de l'interface PricingEngine — c'est l'interdit exprès du sprint. **Chemin de mise en conformité :** la première story qui livre E10.21 complète ces colonnes pour les lignes existantes et nouvelles ; aucune migration de schéma n'est nécessaire, seulement une écriture.

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **p1** | `tests/sql/gescom-e10-3-commercial-quotes.sql` (isolation par tenant sur `commercial_quotes`/`commercial_quote_lines`, `with check`, non-duplication du numéro sous appels séquentiels rapides, déni total sur `commercial_quote_number_counters` hors de la fonction) n'a **jamais été exécuté** — même contrainte que B3/n1/o1 (§8.1/§8.4/§8.5), Docker absent sur le poste de rédaction. | Un cas SQL écrit mais jamais lancé peut échouer sur une broutille et donner l'illusion d'une garantie prouvée — en particulier le verrouillage de la ligne du compteur par l'UPSERT, qui est LE mécanisme anti-doublon du CA5. | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé. Le fichier est déjà enregistré dans `SQL_CASES` (`scripts/test-storefront-sql.sh`). |
| **p2** | Le déploiement de la migration `20260901000600_gescom_e10_3_commercial_quotes.sql` sur le projet Supabase partagé n'a pas pu être effectué dans cette session : ni le CLI `supabase` (PAT absent, `supabase projects list` renvoie `Unauthorized`), ni `psql`/`DATABASE_URL` direct ne sont disponibles. | L'endpoint fonctionne en mémoire (tests de contrat) et compile (`pnpm typecheck`/`pnpm build`/`deno check` sur l'edge function), mais n'a pas encore de preuve d'exécution réelle contre Postgres, ni de disponibilité pour Studio. | Régénérer un PAT Supabase (cf. CLAUDE.md) et lancer `supabase db push` sur le projet `ightkxebexuzfjdbpsdg`, comme pour E10.1/E10.4/E10.5. |
| **p3** | Une notion de « devis » existe déjà en base depuis `20260418000003` (étendue par `20260702000100`, S-QUOTES-1) et sert le workflow storefront de `src/modules/quotes/` : table `public.quotes`/`public.quote_lines`, adressée par `/api/v1/tenants/{tenantId}/quotes` (tenant au chemin, contraire au CA4 du socle E10.0), client en texte libre sur une table `clients` déjà supprimée, numérotation libre non garantie unique, aucune colonne `project_id`, colonnes de marge maison (`unit_cost_ht`/`unit_price_ht`/`margin_pct`). C'est un modèle **incompatible**, pas une variante : E10.3 crée donc `commercial_quotes`/`commercial_quote_lines` plutôt que d'étendre `quotes`/`quote_lines`. Documenté en détail dans l'en-tête de la migration et de `src/modules/commercial-quotes/api/contracts.ts`. Confirmé par `qa-review` — vérifié ligne par ligne, avec deux arguments plus forts que ceux d'origine : `quotes.user_id` porte un `ON DELETE CASCADE` vers `auth.users` (une commande atelier disparaîtrait si l'auteur quittait le tenant), et les contraintes `NOT NULL` du modèle E10.3 (`customer_id`, `project_id`) seraient inapplicables sur les lignes legacy existantes. | Un lecteur pressé du contrat OpenAPI ou du code pourrait croire à un doublon fonctionnel de `quotes`. | Aucune action requise : les deux systèmes sont durablement distincts (bounded contexts différents — devis atelier Gestion commerciale vs demande de devis boutique storefront). Si une convergence est un jour souhaitée, elle relève d'une décision produit explicite, pas d'un refactor silencieux. |
| **p4** | `updateQuote` (PATCH) n'a aucun garde de statut, alors que sa description au contrat dit « modifie l'entête d'un devis **brouillon** ». Sans effet aujourd'hui — cette story ne fait transiter aucun devis hors de `draft` — mais dès qu'un statut `sent`/`accepted`/`rejected`/`converted` existera réellement (E10.10, E10.12), un devis envoyé resterait modifiable par `PATCH`. | Un commercial pourrait changer la validité ou l'affichage des remises d'un devis déjà transmis au client, sans que rien ne le signale. | Ajouter la garde de statut à `CommercialQuotesService.update()` (refus `quote.update_requires_draft` si `current.status !== 'draft'`) au plus tard avec E10.10 ou E10.12, qui sont les premières stories à produire un statut autre que `draft`. |
| **p5** | Le contrat déclare `item_ids` avec `uniqueItems: true`, mais le schéma Zod (`createQuoteFromProjectCommandSchema`) ne porte que `.min(1)` : un doublon d'id dans la requête n'est pas rejeté à la validation de forme, il finit refusé par la fonction Postgres en 422 `quote.items_invalid` (comptage `count(*)` ≠ longueur du tableau incluant le doublon) plutôt qu'en 400 `api.validation_failed`. Pas un défaut de sécurité (le doublon est bien refusé), juste un code d'erreur moins précis que ce que promet le contrat. | Un client qui envoie deux fois le même `item_id` par erreur reçoit un message métier (« éléments invalides ») au lieu d'un message de forme (« doublon interdit »), plus difficile à corriger côté intégrateur. | Ajouter `.refine()` (ou `z.array(uuidSchema).min(1)` avec une contrainte d'unicité explicite) à `createQuoteFromProjectCommandSchema` pour aligner le code sur le contrat, lors d'une prochaine évolution de cet endpoint. |
| **p6** | Le handler d'erreur de la fonction Postgres (`exception when invalid_text_representation then raise exception 'invalid_item_ids: malformed uuid'`) affiche toujours ce message, même quand c'est un cast **numérique** (`quantity`/`production_price`, sur un `quote_payload` corrompu) qui a échoué, pas un cast d'UUID. Diagnostic trompeur, pas un défaut de sécurité : l'appelant reçoit un 422 dans les deux cas, jamais un 500. | Un support qui déboguerait un 422 sur un projet dont un `quote_payload.quantity` est mal formé serait orienté vers la mauvaise piste (« UUID invalide ») par le message d'erreur. | Distinguer les deux origines dans la fonction (bloc `exception` séparé, ou message générique ne présumant pas de la colonne fautive) lors d'une prochaine évolution de cette fonction. |
| **p7** | Le repli `quote_payload.amounts.price` utilisé par `production_price` (CA3, quand `clariprint_price_ht` est absent) peut être un **prix marché heuristique** (`estimateMarketPriceHT()`, voir `priceResolver.ts`) plutôt qu'un vrai coût de production Clariprint — la colonne s'appelle `production_price` mais peut contenir une estimation commerciale. Signalé par `qa-review`, à arbitrer, **pas à corriger dans cette story** : il n'existe aujourd'hui aucune troisième valeur disponible dans `quote_payload.amounts` qui distinguerait les deux origines. | Un devis dont un élément n'a jamais eu de vrai chiffrage Clariprint afficherait un « prix de production » qui est en réalité une estimation, sans distinction visuelle ni au contrat. | À trancher à la livraison d'E10.21 (PricingEngine), qui est le bon endroit pour introduire une distinction explicite de provenance (`source: 'clariprint' \| 'prix_marche'`) sur les colonnes de prix, plutôt que de la bricoler maintenant sur une colonne qui va de toute façon être réécrite par cette story future. |

**Corrigé pendant la revue plutôt que tracé (qa-review) :** `tests/sql/gescom-e10-3-commercial-quotes.sql` relisait l'id du devis du tenant A et l'état du compteur de numérotation **en les requêtant sous le rôle restreint** (`authenticated`, réduit au tenant B) — exactement la RLS que le test prétendait vérifier. Sous ce rôle, ces lectures rendaient `NULL` : le contrôle négatif de lecture sur `commercial_quote_lines` comptait `where quote_id = NULL` (toujours 0, quelle que soit la policy), le contrôle négatif d'écriture ciblait `where id = NULL` (0 ligne affectée, même défaut), le contrôle final de non-modification était vacant par construction (l'`UPDATE` n'avait jamais rien ciblé), et le contrôle « le compteur n'avance pas si la création échoue » comparait `NULL` à `NULL` (jamais pris). **Piège à connaître, même famille que m4/m5 (E10.4, §8.3) et C5 (E10.1, §8.5) :** un test de RLS qui a besoin d'un identifiant ou d'une valeur comme **oracle** de comparaison doit le figer **pendant la phase privilégiée** (rôle de connexion `postgres`, qui contourne la RLS), jamais le rederiver par une requête sous le rôle restreint que le test est censé mettre en défaut — sans quoi le test reste vert même si la policy sous-jacente est supprimée ou relâchée. Corrigé en reprenant le patron déjà en place dans `tests/sql/gescom-e10-1-projects.sql` : capture de `quote_a1` dans la table de contexte temporaire dès sa création (encore sous `postgres`), et déplacement des scénarios 1/2/3/7 — qui n'exercent aucune RLS, la fonction `security definer` s'exécutant avec les droits de son propriétaire quel que soit l'appelant — entièrement en phase privilégiée, avant le premier `set local role authenticated`.

### 8.7 Dette signalée sur E10.6 (Référentiel des règles de prix), acceptée

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **q1** | `tests/sql/gescom-e10-6-price-rules.sql` (coherence scope/cibles, ordre des périodes, isolation par tenant sur `price_rules` ET `product_range_default_margins`, journal d'audit, append-only de `price_rules_audit` via `information_schema.table_privileges`) n'a **jamais été exécuté** — même contrainte que B3/n1/o1/p1 (§8.1/§8.4/§8.5/§8.6), Docker absent (`docker: command not found`) sur le poste de rédaction. | Un cas SQL écrit mais jamais lancé peut échouer sur une broutille et donner l'illusion d'une garantie prouvée — en particulier la contrainte `CHECK` d'équivalence scope↔cibles, qui est LE mécanisme anti-incohérence du CA2, et le trigger d'audit qui distingue `activated`/`deactivated`/`updated`. | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé. Le fichier est déjà enregistré dans `SQL_CASES` (`scripts/test-storefront-sql.sh`). |
| **q2** | Le déploiement de la migration `20260902000200_gescom_e10_6_price_rules.sql` sur le projet Supabase partagé n'a pas pu être effectué dans cette session : ni le CLI `supabase` (PAT absent), ni `psql`/`DATABASE_URL` direct ne sont disponibles. | L'endpoint fonctionne en mémoire (tests de contrat, tests unitaires de service) et compile (`pnpm typecheck`), mais n'a pas encore de preuve d'exécution réelle contre Postgres, ni de disponibilité pour Studio. | Régénérer un PAT Supabase (cf. CLAUDE.md) et lancer `supabase db push` sur le projet `ightkxebexuzfjdbpsdg`, comme pour E10.1/E10.3/E10.4/E10.5. |
| **q3** | La description de `listPriceRules` (commentaire du contrat, pas le schéma) dit qu'un `sort` différent de celui encodé dans un curseur en cours « rend 400 `api.validation_failed` » ; l'implémentation rend **422**, via le même helper `validationFailed()` que **tout** le reste de la façade (`api.invalid_page_params`, `api.invalid_cursor`, `customer.siret_invalid`, etc. — jamais 400 hors corps JSON malformé). Le contrat déclare les DEUX réponses possibles (400 et 422) pour cet endpoint : 422 n'est donc pas une violation du schéma, seulement un écart avec la valeur littérale de la prose. | Un client qui brancherait son comportement sur le code HTTP exact (400) plutôt que sur le code métier (`api.validation_failed`) serait surpris. | Si un désaccord de fond est confirmé, corriger la PROSE du contrat (agent `architecte`) pour dire 422, plutôt que de introduire une deuxième famille de validation de requête à 400 dans la façade E10 — ce lot n'a pas tranché la question produit, seulement suivi la convention déjà en place partout ailleurs. |

---

### 8.8 Dette signalée sur E10.7 (Arbitrage des règles de prix concurrentes), acceptée

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **r1** | `tests/sql/gescom-e10-7-price-rules-resolve.sql` (algorithme complet de `resolve_price_rule` — specificité puis récence, motif `specificity`/`recency`, non-écriture de la règle perdante, cas `null`, et isolation par tenant AVEC attaque directe par `p_tenant_id` forcé) n'a **jamais été exécuté** — même contrainte que B3/n1/o1/p1/q1 (§8.1/§8.4/§8.5/§8.6/§8.7), Docker absent (`docker: command not found`) sur le poste de rédaction. | Un cas SQL écrit mais jamais lancé peut échouer sur une broutille et donner l'illusion d'une garantie prouvée — en particulier le scénario 7, qui est LE test qui prouve que `p_tenant_id` ne peut pas être détourné pour lire un autre tenant malgré la fonction `SECURITY INVOKER`. | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé. Le fichier est déjà enregistré dans `SQL_CASES` (`scripts/test-storefront-sql.sh`). |
| **r2** | Le déploiement de la migration `20260902000300_gescom_e10_7_resolve_price_rule.sql` sur le projet Supabase partagé n'a pas pu être effectué dans cette session : ni le CLI `supabase` (PAT absent, `supabase projects list` renvoie `Unauthorized`), ni `psql`/`DATABASE_URL` direct ne sont disponibles. | L'endpoint fonctionne en mémoire (tests de contrat contre un fake qui réimplémente l'algorithme) et compile (`pnpm typecheck`), mais n'a pas encore de preuve d'exécution réelle de la fonction SQL contre Postgres, ni de disponibilité pour Studio/Clariprint. | Régénérer un PAT Supabase (cf. CLAUDE.md) et lancer `supabase db push` sur le projet `ightkxebexuzfjdbpsdg`, comme pour E10.1/E10.3/E10.4/E10.5/E10.6. |

### 8.9 Dette signalée sur E10.21 (Interface PricingEngine), traitée au round `qa-review` "Changes Requested"

**Dérogation p7 (§8.6) — RÉSOLUE, pas re-reportée.** p7 assignait explicitement à E10.21 le soin d'introduire une distinction de provenance sur les colonnes de prix (`source: 'clariprint' | 'prix_marche'`), plutôt que de la bricoler sur une colonne de `commercial_quote_lines` qui serait de toute façon réécrite par cette story. E10.21 introduit ce champ, additif des deux côtés :

- `CostInputPost.source?: CostSource` (entrée, optionnel — un appelant historique qui ne le fournit pas n'est pas cassé) ;
- `PricedLineBreakdownItem.source: CostSource` (sortie, toujours présent, propagé depuis le premier poste d'entrée qui en précise un, `'clariprint'` par défaut sinon) ;
- `CostSource = 'clariprint' | 'prix_marche'` : sous-ensemble volontaire des 4 valeurs de `PriceSource` (`src/modules/clariprint/ui/helpers/priceResolver.ts`) — `'library_cached'` et `'zero'` concernent la résolution du **prix affiché**, pas la provenance d'un **coût** déjà arrêté en entrée du moteur, donc hors périmètre ici.

Choisi plutôt qu'un nouveau report car c'est un ajout de champ optionnel (aucun appelant existant, donc aucune rupture) et que l'implémentation provisoire (`SingleCostPricingEngine`, breakdown agrégé en un seul élément `total`) peut porter cette provenance sans ambiguïté dès aujourd'hui. **Limite documentée** : tant que le breakdown reste agrégé en un seul poste, des postes d'entrée aux provenances différentes ne peuvent pas être distingués en sortie (le premier poste explicite l'emporte) — une vraie décomposition par poste (E10.8 dégelée) portera une `source` par élément, sans cette ambiguïté. **Reste hors périmètre d'E10.21** : câbler ce champ sur `commercial_quote_lines` (p7 concernait `production_price`, alimenté aujourd'hui par une donnée figée d'E10.3, cf. §8.6) — à faire par la première story qui branche réellement `PricingEngine` sur `commercial_quote_lines` (E10.9/E10.12/E10.16/E10.19), pas par E10.21 qui ne câble rien.

**Prose OpenAPI périmée — signalé, non corrigé ici (hors domaine `dev-story`).** `openapi/magrit-core.v1.yaml:1314-1318` et `:3765-3768` décrivent encore `breakdown` comme vide tant qu'E10.21 n'est pas livrée. E10.21 est désormais livrée (implémentation provisoire `SingleCostPricingEngine`, jamais de breakdown vide) mais n'est câblée sur aucune ressource réelle : la phrase du contrat reste donc vraie en pratique (`QuoteLine.breakdown` réel est toujours `'[]'`, cf. §8.6) mais sa justification a changé — ce n'est plus « E10.21 n'existe pas », c'est « rien ne l'appelle encore ». À mettre à jour par l'agent `architecte` lors du câblage réel d'E10.9/E10.12/E10.16/E10.19 sur `QuoteLine`, pas avant : corriger la prose maintenant sans câblage réel remplacerait une phrase vraie par une autre approximation.

**Documentation interne à l'implémentation (pas au contrat) — corrigée.** `pricing-engine.ts` renvoyait vers un « rapport de fin de story » qui n'est pas un fichier versé au dépôt (les rapports de fin de story sont un livrable transitoire vers l'agent `scribe`/Notion, jamais un artefact du code). Les renvois pointent désormais vers cette section.

### 8.10 Unification des devis (chantier post Sprint 5, dev-story), acceptée

**Décision produit (Arnaud, PDG/product owner) — verbatim :** « L'existant ne relève que de données de tests donc n'a aucune importance, ne pas le migrer, le supprimer purement et simplement » ; « Nous supprimons l'ancien module pour ne conserver que le nouveau auquel nous associons les IHM existantes » ; « Un devis est un devis, qu'il soit initié par un client côté boutique ou réalisé par un commercial depuis Magrit home, au final ce dernier devra le gérer depuis l'outil de gestion commerciale » ; « Nous nous sommes d'abord attaché au workflow du devis principal, celui créé par le commercial ; nous détaillerons plus avant celui issu des boutiques plus tard, et dans l'intervalle alignement des 2 ».

**Ce qui a été fait.** Le module `src/modules/quotes/` (table legacy `public.quotes`/`public.quote_lines`, façade historique `/api/v1/tenants/{tenantId}/quotes`, jamais décrite dans `openapi/magrit-core.v1.yaml` — donc aucune modification du contrat n'était nécessaire ni possible ici) a été supprimé purement et simplement : migration `20260902000400_gescom_devis_unification_drop_legacy_quotes.sql` (DROP des tables, de la fonction de trigger dédiée, et des colonnes de liaison `orders.quote_id`/`pim_candidates.source_quote_id`, toutes deux à 0 ligne non-nulle), module applicatif, routes serveur, repository Supabase, et toute IHM couplée au schéma legacy. `commercial_quotes`/`commercial_quote_lines` (E10.3) est désormais l'UNIQUE système de devis. Les écrans `QuotesPage`/`QuoteEditorPage` existants ont été **portés** sur le nouveau backend plutôt que réécrits : la liste (`src/modules/commercial-quotes/ui/workspace/QuotesPage.tsx`, route `commercial-quotes.workspace.list` sur `GET /api/v1/quotes`, déjà décrit au contrat par E10.3) était le seul écran manquant ; l'éditeur (`QuoteEditorPage.tsx`, déjà livré par E10.3) n'a pas été remplacé. Les helpers non couplés au schéma (`quoteMath.ts`, rendu HTML imprimable + gabarits `builtin`) ont été relocalisés (`orders/ui/helpers/quoteMath.ts`, `quote-templates/ui/helpers/quote-rendering.ts`) plutôt que dupliqués ou perdus — voir l'en-tête de chaque fichier pour la justification du nouveau propriétaire.

**Concept retiré, pas d'équivalent forcé.** L'ancien système portait une notion de « devis en attente de validation » (`PendingQuotesPage`, statut applicatif `draft/sent/pending`). Le statut `commercial_quotes.status` (`draft | sent | accepted | rejected | converted`) n'a pas de distinction « en attente d'une validation par un tiers » : c'est un pipeline commercial linéaire, pas un pipeline d'approbation. Plutôt que de forcer un mapping arbitraire (ex. `draft` = « en attente »), l'écran et son point de navigation ont été retirés sans remplacement. Si un futur besoin de « devis à traiter en priorité » émerge, il relève d'une décision produit explicite sur le sens réel de ce filtre (délai de validité proche ? envoyé sans réponse depuis N jours ?), pas d'une reconduction mécanique de l'ancien libellé.

**Fonctionnalité perdue, assumée.** L'ancien système permettait d'historiser un « snapshot imprimé » d'un devis ad hoc (bouton Imprimer depuis une `ProductCard` ou un panier) dans la table `quotes`, en dehors de tout projet. Cette historisation n'a pas d'équivalent dans `commercial_quotes`, qui exige un `project_id` réel (CA4 d'E10.3 : un devis n'existe pas sans projet). Les deux points d'impression concernés (`catalog/ui/components/QuoteModal.tsx`, `orders/ui/components/CartButton.tsx`) continuent d'imprimer un devis HTML à la volée, mais n'historisent plus rien — cohérent avec la décision n°3 d'Arnaud (un devis réel se gère désormais exclusivement depuis l'outil de Gestion commerciale, donc depuis un projet). Le panier (`CartContext`/`CartButton`) était déjà retiré de toutes les surfaces internes Magrit depuis E10.1 (qa-review B1, `tests/e2e/atelier-no-cart-button.spec.ts`) : seule sa création de devis multi-lignes (`createQuoteFromCart`, backend legacy) a été retirée du composant, qui reste par ailleurs du code mort (jamais monté dans l'arbre de rendu).

**Point de vigilance signalé par qa-review — numérotation `makeQuoteReference()` non tranchée.** `src/modules/quote-templates/ui/helpers/quote-rendering.ts` (`makeQuoteReference()`) génère côté navigateur un numéro `DEV-AAAA-xxxxxx` (à partir de `Date.now()`) au même FORMAT VISUEL que la numérotation transactionnelle serveur `commercial_quotes.number` (`DEV-AAAA-NNNNN`, générée par `api_create_commercial_quote_from_project_items`, E10.3). Cette fonction sert au rendu HTML imprimable d'un devis ad hoc (`QuoteModal`, `CartButton`) qui n'historise plus rien depuis ce chantier — le numéro qu'elle produit n'est donc jamais écrit dans `commercial_quotes`, mais un document imprimé portant ce numéro est visuellement indiscernable d'un vrai numéro de devis engageant, avec un risque de confusion pour le destinataire (support, comptabilité du client) et de collision de format si les deux se retrouvent un jour dans le même classeur. Décision produit à trancher par Arnaud (préfixe distinct, ex. `EST-` pour une estimation non contractuelle ; mention explicite « document non contractuel » sur le rendu HTML ; ou suppression pure et simple de cette fonction si l'impression ad hoc doit elle aussi passer par `commercial_quotes`) — non traité ici, aucune correction de code appliquée par ce chantier.

**Point d'entrée boutique — à concevoir dans une story future.** Le point 4 de la décision d'Arnaud (« nous détaillerons plus avant celui issu des boutiques plus tard ») n'a PAS été construit ici : l'ancienne route `quotes.storefront.create` (`availability: 'planned'`, jamais implémentée) a simplement été retirée avec le reste du module, sans être recréée sur `commercial_quotes`. **Obstacle de schéma réel à lever pour cette story future :** `commercial_quotes.project_id` est `NOT NULL` (contrainte posée par la migration `20260901000600_gescom_e10_3_commercial_quotes.sql`, cohérente avec CA4 d'E10.3 — un devis hérite du client et du projet source). Un devis initié depuis une boutique par un client n'a, par construction, pas d'Espace Projets (E10.1) associé à ce moment-là : soit cette story future crée un projet implicite au moment de la demande boutique, soit elle assouplit la contrainte (`project_id` nullable + logique de rattachement a posteriori). Les deux options ont un impact sur la fonction `api_create_commercial_quote_from_project_items` et sur le contrat `createQuoteFromProjectCommand` (`project_id` actuellement requis) — à trancher par l'agent `architecte` avant tout code, pas par un contournement applicatif.

### 8.11 E10.11 (droit dédié `can_manage_pricing`) — contrat et implémentation livrés

**Ce que la story solde.** E10.6 (CA7) et E10.9 avaient posé une garde grossière **documentée comme dette** : « rôle `admin` du tenant », en attendant un droit métier dédié. Le contrat annonçait explicitement son remplacement. C'est fait : `can_manage_pricing` est déclaré dans `x-magrit-capabilities` et exigé par `x-required-capabilities` sur les quatre opérations concernées (mécanisme complet en §3.5).

| Opération | Chemin | Avant | Après |
|---|---|---|---|
| `listQuoteAuditEntries` | `GET /quotes/{quoteId}/audit-entries` | garde applicative « rôle `admin` » (E10.9) | `can_manage_pricing` |
| `createPriceRule` | `POST /price-rules` | **aucune garde d'habilitation** — RLS `role in ('admin','member')` | `can_manage_pricing` |
| `updatePriceRule` | `PATCH /price-rules/{priceRuleId}` | idem | `can_manage_pricing` |
| `setProductRangeDefaultMargin` | `PUT /product-ranges/{productRangeId}/default-margins` | idem | `can_manage_pricing` |

**Deux natures de changement dans ce tableau, à ne pas confondre.** Sur `listQuoteAuditEntries`, c'est une **substitution additive** : la dérivation `admin → tous les droits` (§3.5) garantit que personne ne perd l'accès. Sur les trois écritures du référentiel de prix, c'est une **fermeture** : la RLS d'E10.6 les ouvre aujourd'hui à *tout membre*, plus largement que ce que le CA7 d'E10.6 exigeait déjà pour son écran. L'API y était plus permissive que l'intention produit ; E10.11 aligne l'API sur l'intention, ce n'est pas un durcissement gratuit — mais un membre simple qui écrivait des règles de prix par appel direct recevra désormais 403.

**Ce que la story livre, et ce qu'elle ne livre PAS — écart assumé, tranché par Arnaud le 04/09.** Elle livre un **durcissement RLS réel** : les gardes `tm.role in ('admin','member')` et les policies de lecture d'audit filtrées par le seul tenant sont remplacées par une vérification de `public.user_has_capability(tenant_id, 'can_manage_pricing')`. Elle ne livre **pas** de mécanisme de délégation : `can_manage_pricing` reste détenu par les seuls `admin` du tenant, par dérivation d'appartenance. Le verrou est le trigger UM1 décrit en §3.5 — une définition de rôle *peut* porter `{"can_manage_pricing": true}`, mais son **affectation** à un membre Magrit (`access_scope = 'magrit_full'`) est rejetée, faute d'être l'une des deux options produit. Les deux pistes de contournement (option système `option_pricing`, élargissement du trigger UM1) ont été **explicitement écartées** au profit du maintien de « admin unique » (chantier UM, 14/08). Toute rédaction de contrat qui laisserait entendre le contraire est un défaut à corriger, pas une simplification acceptable.

**Lectures volontairement non gardées** (`listPriceRules`, `getPriceRule`, `resolvePriceRule`, `getProductRangeDefaultMargin`) : `resolvePriceRule` et `getProductRangeDefaultMargin` sont les entrées du `PricingEngine` (E10.21). Les fermer empêcherait un commercial ordinaire de chiffrer une affaire, et il n'existe aucune notion de « droit de chiffrer » dans ce sprint. Un commercial doit pouvoir voir **quelle** règle s'applique à son devis — c'est ce qui rend le prix explicable au client. S'y ajoute §7 : restreindre une lecture déjà publiée en v1 n'est pas autorisé.

**Frontière avec E10.9, à ne pas franchir.** `can_manage_pricing` ne conditionne **pas** `updateQuoteLine` : arbitrer un prix sur une affaire est le geste commercial ordinaire, et « l'autorisation hiérarchique est hors périmètre V1 » (CA7 d'E10.9, amendé au WM du 01/09). Seule la **lecture du journal** est gardée, parce que lire ce que les autres ont consenti est une position de supervision. Étendre le droit à l'édition réintroduirait par la bande l'autorisation hiérarchique explicitement sortie de la V1.

**Ce que l'implémentation a livré (dev-story), au-delà du câblage de la façade :**

| Réf. | Point | Statut |
|---|---|---|
| **s1** | **Policy RLS** sur `price_rules` et `product_range_default_margins` fondée sur `public.user_has_capability(tenant_id, 'can_manage_pricing')`, en remplacement de `tm.role in ('admin','member')`. | Fait — `supabase/migrations/20260904142026_gescom_e10_11_can_manage_pricing.sql`, cas SQL `tests/sql/gescom-e10-11-can-manage-pricing.sql` (voir dette **t1** ci-dessous : jamais exécuté, Docker absent du poste). |
| **s2** | **Seed du droit** : aucune définition de rôle existante ne porte `can_manage_pricing`, et aucune ne pourrait être affectée à un membre Magrit si elle le portait (trigger UM1, §3.5). | Non traité, **assumé et tranché** (Arnaud, 04/09, qa-review round 2 / B1) : `can_manage_pricing` reste réservé à l'`admin` du tenant par dérivation d'appartenance, sans mécanisme de délégation dans cette story. Cohérent avec « admin unique » (chantier UM, 14/08). Les deux alternatives (option système `option_pricing`, élargissement du trigger UM1) sont **écartées**, pas différées à un arbitrage ultérieur. Le contrat a été corrigé en conséquence : il ne promet plus de délégation nulle part (bloc E10.11 de `securitySchemes`, `x-magrit-capabilities.can_manage_pricing`, réponse `ForbiddenCapability`, descriptions des 4 opérations gardées). |
| **s3** | **Nom de la capability côté UI** : `can_manage_pricing` utilisé **directement** comme identifiant de capability dans `pricing/manifest.ts`/`surface-contributions.ts`, sans passer par `WORKSPACE_CAPABILITY_ALIASES` (aucune entrée requise, le nom est déjà celui de la base). | Fait. |
| **s4** | `requiredTenantRole: 'admin'` sur `pricing.workspace.rules` → `requiredCapabilities: ['can_manage_pricing']`, et `capabilities` de `pricing/manifest.ts` déclare désormais le droit. | Fait — `src/modules/pricing/surface-contributions.ts`, `src/modules/pricing/manifest.ts`. |
| **s5** | Retiré `findActorTenantRole()` du port `CommercialQuotesRepository`, remplacé par `actorHasCapability(tenantId, actorId, capability)` ; `role !== 'admin'` de `commercial-quotes-service.ts` remplacé par l'évaluation du droit. Le faux repository de test (`tests/contract/_fakes/commercial-quotes-repository.fake.ts`) et le test de contrat (`listQuoteAuditEntries`) suivent la nouvelle signature. **Étendu au même titre à `PriceRulesRepository`** : `actorHasCapability()` ajouté au port (`src/modules/pricing/application/price-rules-repository.ts`), implémenté dans `SupabasePriceRulesRepository` (même appel RPC `user_has_capability`) et le faux de test. `PriceRulesService.create()/update()/setDefaultMargin()` appellent une garde privée `assertCanManagePricing()` AVANT toute autre validation, et lèvent `PriceRuleAccessDeniedError` (nouveau, `price-rules-repository.ts`) — traduite en 403 `identity.role_required` par `price-rules-routes.ts` via `roleRequired(['can_manage_pricing'])`. Cette garde applicative n'était **pas optionnelle** : sans elle, un refus RLS sur `price_rules`/`product_range_default_margins` (s1) remonterait comme une erreur Postgres non reconnue par `toDomainError()`, donc un 500 `api.internal_error` — pas le 403 `ForbiddenCapability` promis par le contrat. `update()` a dû gagner un paramètre `actor: UserId` (absent jusqu'ici, aucun appelant HTTP hors `price-rules-routes.ts` ne l'invoquait) ; les 6 appels de `tests/modules/pricing/price-rules-service.test.ts` suivent. Test dédié : `tests/contract/price-rules.contract.test.ts` (« E10.11 — droit dédié can_manage_pricing sur les écritures du référentiel »), cycle refus→lecture toujours ouverte→octroi sur les trois écritures, plus `tests/modules/pricing/price-rules-service.test.ts` (« garde can_manage_pricing (E10.11) ») au niveau service. | Fait. |
| **s6** | **Lint `lintRequiredCapabilities()`** dans `tests/contract/_lint.ts`, symétrique de `lintRequiredScopes()` : refuse un `x-required-capabilities` absent de `x-magrit-capabilities`, et refuse qu'une opération gardée par un droit soit joignable par `serviceKey`. | Fait pour les deux règles document-level, testé dans les deux sens (`openapi-document.contract.test.ts`). **Non fait** : l'alignement code ↔ contrat dans `lintRoutesAgainstContract()` — contrairement aux scopes (`requiredScopes` sur `GescomRoute`, appliqué par `assertScopes()`), il n'existe aujourd'hui **aucun champ déclaratif générique `requiredCapabilities` sur une route de la façade** : l'application du droit est portée par une vérification explicite dans le service, pour les QUATRE opérations (`listQuoteAuditEntries`, `createPriceRule`, `updatePriceRule`, `setProductRangeDefaultMargin`), jamais par le middleware `gescom-middleware.ts`. Ajouter ce champ aurait exigé de faire porter au middleware une notion d'autorisation qu'il ne connaît pas aujourd'hui (il ne connaît que des scopes de clé de service), un changement plus large que cette story — la RLS (s1) reste la barrière de dernier recours en cas d'oubli d'un appel à la garde applicative, mais un tel oubli ne serait plus détecté qu'en 500 (voir remarque s5 ci-dessus), pas en 403. **Chemin de mise en conformité** : si une future story ajoute une garde de capability supplémentaire, introduire `requiredCapabilities?: readonly string[]` sur `GescomRoute`/`defineGescomRoute()`, l'y faire évaluer (même schéma que `assertScopes`, mais contre `user_has_capability`), puis étendre `lintRoutesAgainstContract()` en symétrie de son traitement des scopes. |

**Écart assumé, à ne pas prendre pour un oubli** : l'écran des règles de prix est gouverné *en entier* par `can_manage_pricing` (s4) alors que les lectures qu'il consomme restent ouvertes à tout membre par l'API. C'est la séparation normale entre garde d'ergonomie et garde d'autorisation (§3.5, règle 5).

**Tranché par défaut faute de décision produit explicite, repris tel quel de la spécification de l'architecte** : (a) le code d'erreur reste `identity.role_required` plutôt qu'un `identity.capability_required` plus juste lexicalement mais cassant (§3.5, règle 3) ; (b) les lectures du référentiel de prix restent ouvertes à tout membre ; (c) `can_manage_pricing` couvre à la fois la politique tarifaire **et** le journal d'audit des prix, plutôt que deux droits séparés.

**Dette introduite par cette story :**

| Réf. | Trou | Pourquoi c'est un risque | Chemin de mise en conformité |
|---|---|---|---|
| **t1** | `tests/sql/gescom-e10-11-can-manage-pricing.sql` (cycle refus → octroi → révocation du droit sur `price_rules`/`product_range_default_margins`, acteur `role = 'member'` sans dérivation admin) n'a **jamais été exécuté** — même contrainte que B3/n1/o1/p1 (§8.1/§8.4/§8.5/§8.6), Docker absent (`docker: command not found`) du poste de rédaction. | Un cas SQL écrit mais jamais lancé peut échouer sur une broutille et donner l'illusion d'une garantie prouvée — en particulier ici, où la garantie est un **resserrement de sécurité réel** (un appel PostgREST direct par un membre simple est désormais censé être refusé). | Lancer `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé. Le fichier est déjà enregistré dans `SQL_CASES` (`scripts/test-storefront-sql.sh`). |
| ~~**t2**~~ | ~~Le déploiement des migrations `20260904142026_gescom_e10_11_can_manage_pricing.sql` et `20260904150000_gescom_e10_11_audit_select_capability.sql` sur le projet Supabase partagé n'avait pas pu être effectué faute de PAT accessible en session.~~ **Levée le 2026-09-05.** PAT fourni par Arnaud, `supabase db push --linked --dry-run` (les deux migrations listées, rien d'autre) puis `supabase db push --linked` sur `ightkxebexuzfjdbpsdg`, `supabase migration list --linked` confirme `local = remote` sur les deux entrées (`20260904142026`, `20260904150000`) ainsi que sur tout l'historique antérieur. La RLS durcie (écriture + lecture d'audit) est active en production. | — | — |
| **t3** | Voir s6 ci-dessus : `lintRoutesAgainstContract()` n'aligne pas encore le code sur `x-required-capabilities`, faute d'un champ déclaratif de capability sur `GescomRoute`. | Une route qui retirerait sa garde applicative (le check `actorHasCapability` dans le service, ou une future garde de ce type sur une autre route) sans que le contrat ne change ne serait détectée par aucun test de contrat — seulement, le cas échéant, par un test comportemental dédié (ceux déjà écrits pour `listQuoteAuditEntries` et pour les trois écritures du référentiel de prix). | Décrit dans la cellule s6 ci-dessus. |

**Corrigé au round `qa-review` 1 (bloquant B1, réserve R5)**, sur le commit `1568143` ci-dessus.

- **Bloquant B1.** Les policies de LECTURE `commercial_quote_line_audit_select` (E10.9, `20260904000100_gescom_e10_9_quote_line_discounts.sql:517-525`) et `price_rules_audit_select` (E10.6, `20260902000200_gescom_e10_6_price_rules.sql:268-272`) ne filtraient QUE par isolation tenant, jamais par `can_manage_pricing`. La garde s5 ci-dessus (`listAuditEntries()` évalue la capability avant toute lecture côté API) ne fermait donc que le chemin API : un membre simple du tenant, sans `can_manage_pricing`, obtenait 200 sur les deux journaux d'audit par un appel PostgREST direct (`GET .../rest/v1/commercial_quote_line_audit?select=*`), exactement la donnée de supervision que le contrat déclare protégée. Contournement RLS classique — une garde applicative ne vaut que ce que vaut la RLS sous-jacente.
- **Réserve R5.** Même trou sur `price_rules_audit_select`, par cohérence de classe de donnée (avant/après d'une règle de prix), même si aucun endpoint ne l'expose aujourd'hui.
- **Correction.** Nouvelle migration `supabase/migrations/20260904150000_gescom_e10_11_audit_select_capability.sql`, DISTINCTE de `20260904142026_gescom_e10_11_can_manage_pricing.sql` (celle-ci n'était pas encore déployée sur le projet Supabase partagé au moment du correctif — dette t2 ci-dessus — une migration additive séparée, `drop policy if exists` puis `create policy` sur des policies déjà existantes, évite toute ambiguïté sur l'état déployé). Les deux policies exigent désormais `public.user_has_capability(tenant_id, 'can_manage_pricing')` en plus de l'isolation tenant. L'argument d'ouverture d'origine (« une RLS fermée masquerait une absence d'habilitation derrière une page vide », cité au commentaire de policy d'E10.9) ne s'applique plus une fois `can_manage_pricing` livré : la garde applicative produit déjà le refus explicite 403 avant toute lecture sur le chemin API, la RLS fermée ne fait donc que fermer le contournement direct, sans régression de comportement API.
- **Test.** `tests/sql/gescom-e10-11-can-manage-pricing.sql` étendu d'un scénario 4 : un devis + une ligne réels sont créés dans le tenant du scénario pour produire une entrée `commercial_quote_line_audit` véritable (trigger E10.9), puis le cycle refus (acteur `member` sans `can_manage_pricing`, revoqué au scénario 3) → octroi (nouvelle affectation portant `can_manage_pricing`) est rejoué en lecture DIRECTE sur les deux journaux. Même dette **t1** que le reste du fichier : jamais exécuté sur ce poste (Docker absent), même chemin de mise en conformité (`pnpm db:local:start && pnpm test:storefront:sql`).

**Corrigé au round `qa-review` 2 (bloquant B2, réserves R1/R2/R3/R4/R5)**, sur le contrat corrigé par l'architecte (bloquant B1 de ce round, cf. paragraphe « Ce que la story livre... » et cellule s2 ci-dessus, déjà à jour) et sur le commit qui suit `ef15156`.

- **Bloquant B2.** `price_rules_write` et `product_range_default_margins_write` (`20260904142026_gescom_e10_11_can_manage_pricing.sql`) testaient UNIQUEMENT `public.user_has_capability(tenant_id, 'can_manage_pricing')`, sans exiger l'appartenance actuelle au tenant. Or `user_has_capability` (`20260814000200_admin_unique.sql:130-157`) porte une troisième branche qui interroge `tenant_role_assignments`/`tenant_role_definitions` **sans jamais vérifier `tenant_members`** : un utilisateur qui quitte un tenant (`src/adapters/supabase/members-repository.ts` supprime la ligne `tenant_members` mais ne révoque aucune `tenant_role_assignments`, faute de FK/trigger de cascade) conservait donc la capacité d'écrire/supprimer `price_rules`/`product_range_default_margins` de ce tenant par appel PostgREST direct — alors qu'il ne pouvait même plus les **lire** (`price_rules_select` exige `current_user_tenant_ids()`). Régression de sécurité introduite par cette story elle-même par rapport à la policy E10.6 remplacée (`tm.role in ('admin','member')`, qui exigeait de facto une ligne `tenant_members`).
- **Correction.** `tenant_id in (select public.current_user_tenant_ids())` ajouté en tête de chaque `using`/`with check` des deux policies, en symétrie exacte de ce que fait déjà `20260904150000_gescom_e10_11_audit_select_capability.sql` pour la lecture des journaux d'audit. Corrigé **directement dans `20260904142026_gescom_e10_11_can_manage_pricing.sql`** plutôt que par une 3ᵉ migration : ce fichier n'a jamais été déployé sur le projet Supabase partagé (dette t2, confirmée ci-dessus) — la règle « une migration déployée ne change jamais après coup » ne s'applique donc pas ici.
- **Réserve R1.** Le scénario 2 réécrit (voir B1/réécriture du cas SQL ci-dessous) fait désormais la preuve qu'un `admin` **sans aucune affectation de rôle explicite** continue de lire les deux journaux d'audit après le durcissement de `20260904150000` — seul garde-fou anti-régression de ce correctif, qui ne reposait jusqu'ici que sur de la prose.
- **Réserve R2.** Les commentaires citant `20260814000100_droits_owner_admin_par_appartenance.sql:43-74` comme source de `user_has_capability`, ou parlant encore de dérivation `admin`/`owner`, ont été corrigés pour citer `20260814000200_admin_unique.sql:130-157` (la définition **remplacée**, qui diffère : `owner` n'existe plus, `role` est contraint à `admin`/`member`/`partner`) et pour ne plus mentionner `owner` comme valeur possible. Fichiers touchés : `supabase/migrations/20260904142026_gescom_e10_11_can_manage_pricing.sql` (commentaire d'en-tête), `src/modules/pricing/application/price-rules-repository.ts` (deux occurrences), `src/modules/commercial-quotes/application/commercial-quotes-service.ts`, `src/modules/pricing/surface-contributions.ts`. Une occurrence avait échappé à la recherche du round 2 : `src/modules/commercial-quotes/application/commercial-quotes-repository.ts:281` (le PORT, pas l'adaptateur Supabase — celui-ci ne portait effectivement aucune mention `owner`) portait toujours « admin/owner du tenant » ; corrigée en `qa-review` round 2, réserve résiduelle 1, avec la même occurrence oubliée dans `src/modules/pricing/surface-contributions.ts:20-21` (« faculté de déléguer l'accès à un membre porteur d'un rôle qui accorde ce droit », contredisant frontalement le contrat corrigé pour B1).
- **Réserve R3.** `price-rules-service.ts` promettait que la garde `can_manage_pricing` passe avant toute autre validation, mais `updatePriceRule`/`setProductRangeDefaultMargin` (`price-rules-routes.ts`) lisaient la ressource courante (`getById`/`getDefaultMargin`, pour l'`ETag`) **avant** d'appeler le service — un acteur sans le droit pouvait donc recevoir un 404/409 avant le 403 attendu. Ni fuite de donnée (ces lectures sont déjà ouvertes à tout membre, écart assumé documenté ci-dessus) ni bloquant, mais un écart entre la promesse du service et le comportement HTTP réel. **Choix retenu : corriger l'ordre plutôt que le commentaire.** `PriceRulesService.assertCanManagePricing()` devient **publique** (elle restait par ailleurs appelée en interne par `create()`/`update()`/`setDefaultMargin()`, en défense en profondeur pour tout appelant direct du service, ex. les tests unitaires) ; les deux routes l'appellent désormais explicitement **avant** la lecture qui alimente l'`ETag`/`If-Match`, sans toucher au flux de précondition existant. Option la plus simple : aucun nouveau champ déclaratif, aucun changement de signature de contrat, juste un réordonnancement de deux poignées de lignes dans les routes plus une visibilité de méthode.
- **Réserve R4.** `tests/sql/gescom-e10-11-can-manage-pricing.sql` compte désormais, en phase privilégiée, une précondition explicite qui écrit une règle de prix (bypass RLS) et vérifie qu'au moins une entrée `price_rules_audit` en résulte réellement — même pattern que la précondition déjà posée pour `commercial_quote_line_audit` — au lieu de compter sur la chaîne implicite depuis un scénario antérieur (l'écriture réussie du scénario « avec droit » du round précédent, qui n'existe plus sous cette forme après la réécriture du round 2, cf. B1 ci-dessous).
- **Réserve R5.** Story document produit après coup : `_bmad-output/implementation-artifacts/story-E10-11-can-manage-pricing.md`, structure reprise de `story-E10-9-remises-granulaires.md` (produit après coup pour E10.9 par le commit `78ccc03`, pris comme modèle).
- **B1 (contrat, architecte) — conséquence sur le cas SQL.** Le round précédent octroyait `can_manage_pricing` à un acteur `role = 'member'` par une **nouvelle affectation de rôle**. Ce mécanisme échoue désormais volontairement pour tout membre Magrit (`access_scope = 'magrit_full'`, le défaut) : le trigger UM1 `restrict_magrit_assignments_to_options` (`20260824000200_um1_admin_shop_guards.sql:47-74`) n'autorise l'affectation d'un rôle Magrit qu'aux deux options produit (`option_shops`/`option_orders`), jamais à un rôle de tenant arbitraire même porteur d'une capability métier valide — comportement voulu (« admin unique », chantier UM du 14/08), pas un bug à contourner. **Le cas SQL est réécrit** pour prouver ce que la story livre réellement : (1) un acteur `role = 'member'` — jamais admin, aucune affectation — refusé en écriture sur les deux tables et refusé en lecture directe sur les deux journaux, malgré des entrées réelles (préconditions R4) ; (2) un acteur `role = 'admin'` du même tenant, **sans aucune affectation explicite**, accepté sur les deux écritures et les deux lectures par la seule dérivation d'appartenance (couvre aussi R1) ; (3) une tentative explicite d'affectation d'un rôle portant `can_manage_pricing` à un membre Magrit, qui échoue avec `magrit_option_required` (SQLSTATE 23514) — documentée comme comportement à protéger, pas supprimée silencieusement.
- **Dette inchangée.** Toujours **t1** (jamais exécuté, Docker absent du poste) et **t2** (migration `20260904142026` toujours non déployée sur le projet Supabase partagé au moment de ce round — c'est d'ailleurs ce qui a permis de la corriger directement plutôt que par une migration additive). Aucune dette nouvelle introduite par ce round.

### 8.12 E10.10 (remises au-delà de la ligne) — découpage du périmètre et contrat de la première sous-story

**Point de départ.** E10.10 n'avait, avant le 04/09, qu'un intitulé de backlog (`docs/spec/backlog.md:500`). Le cadrage fonctionnel a été obtenu directement d'Arnaud, puis **corrigé et complété par lui le 06/09** après une première rédaction du contrat. État final, six points : (1) le commercial déclenche explicitement l'**envoi** d'un devis — transition `draft → sent`, **impression**, **mise à disposition dans la boutique du client** ; (2) `show_discounts` est un **choix** du commercial, jamais une conséquence automatique de l'envoi ; (3) un devis `sent` ne se **modifie** plus — pour le modifier on le **duplique** — mais il doit pouvoir être **renvoyé** tel quel ; (4) une **remise globale** s'applique à l'ensemble du devis, « soit appliquer un taux, soit formaliser un prix », et elle peut être **négative** (vendre au-dessus du prix public) ; (5) une **durée de validité par défaut** au niveau du tenant, surchargeable devis par devis ; (6) la **TVA s'applique en bas du devis**.

> **Trois corrections d'Arnaud sur la première rédaction, à ne pas réintroduire par inadvertance.**
> 1. La remise globale n'est **pas** « taux OU montant de remise ». Un `global_discount_amount` saisissable réintroduisait au niveau devis le troisième point de vérité que le **CA2 d'E10.9** interdit au niveau ligne (« Remise DÉDUITE, jamais saisie »). Le miroir exact est : on pose un **prix** (`target_net_total`) ou un **taux** (`global_discount_rate`), la remise est **déduite**.
> 2. La remise globale n'est **pas** bornée à `[0, 1]`. Une remise négative est une **majoration**, et c'est un geste commercial légitime — la première rédaction contredisait ce que j'avais moi-même écrit au niveau ligne (`QuoteLine.discount_rate` : « NEGATIVE quand il le majore — les deux sont legitimes »).
> 3. Le renvoi d'un devis déjà `sent` n'est **pas** hors périmètre. Le 409 `quote.send_requires_draft` initialement prévu n'existe plus.

**Découpage retenu — deux sous-stories, pas une.**

| Sous-story | Périmètre | Statut |
|---|---|---|
| **E10.10a** | Statut + envoi (`draft → sent`), garde de statut sur `updateQuote` (solde la dette **p4**, §8.6), duplication, remise globale, totaux serveur. Tout côté **dashboard atelier**. | Contrat écrit (ce paragraphe) |
| **E10.10b** | Mise à disposition du devis dans la **boutique du client** (point 1c). | Non cadrée — à ouvrir avec Arnaud |

**Ce qui fonde la coupure, et ce qui interdit d'en faire trois.** Les quatre gestes d'E10.10a forment une **boucle fermée** qu'on ne peut pas livrer par moitiés sans livrer un piège : geler un devis (2, 3) sans duplication enferme le commercial ; livrer la duplication sans gel ne sert à rien ; la remise globale est la dernière décision commerciale **avant** l'envoi, et c'est l'envoi qui la fige. La garde de statut, elle, n'est testable de bout en bout qu'à partir du moment où une opération produit réellement un statut ≠ `draft` — donc avec l'envoi, pas avant.

E10.10b, au contraire, ne partage **rien** avec E10.10a : acteur différent (`shop_customer_account`, E10.5 — pas un membre du tenant), surface différente (`customer-portal`, dont la contribution devis a été retirée au chantier d'unification, §8.10), RLS à écrire depuis zéro (les policies de `commercial_quotes` ne parlent aujourd'hui que de `current_user_tenant_ids()`). Le seul point de contact est le fait que le devis soit `sent` — c'est-à-dire précisément ce qu'E10.10a livre, et que l'événement `quote.sent` publie.

> **Précision sur l'obstacle attendu, qui n'est pas celui qu'on croyait.** §8.10 identifiait `commercial_quotes.project_id NOT NULL` comme le verrou du point d'entrée boutique. Ce verrou concerne un devis **initié depuis la boutique par un client**, qui n'a pas d'Espace Projets derrière lui. Il ne concerne **pas** le point 1c d'Arnaud, qui est une **lecture seule** par le client d'un devis déjà créé côté atelier : ce devis a un projet, `project_id` est renseigné, la contrainte n'est jamais rencontrée. E10.10b est donc **moins** coûteuse que ne le laissait croire §8.10 ; le vrai travail y est la chaîne d'autorisation `commercial_quotes.customer_id → customers → contacts → shop_customer_accounts`, pas le modèle de données du devis. Les deux sujets restent distincts et ne doivent pas être fusionnés par commodité.

**Hors périmètre des deux, et pourquoi — l'impression (point 1b).** Aucun mécanisme de génération de document **côté serveur** n'existe dans le dépôt (recherche `pdf`/`print`/`document` : aucune dépendance de rendu, aucune Edge Function). L'existant est un rendu **HTML imprimé par le navigateur** — `openQuotePrint()` / `buildQuoteDocumentHtml()` dans `src/modules/quote-templates/ui/helpers/quote-rendering.ts`, avec les gabarits `quote_templates` — hérité du module `quotes` supprimé, et **jamais branché sur `commercial_quotes`** (ses appelants actuels sont `QuoteModal` et `CartButton`, côté catalogue). Le brancher sur `QuoteDetail` est un travail d'**interface** qui n'appelle aucun endpoint nouveau ; ce qui lui manquait pour être écrit sans calcul métier dans le navigateur, ce sont les **totaux serveur** (`Quote.totals`) et le choix d'affichage (`show_discounts`) — que le contrat publie ici. Un **vrai document** (PDF stocké, horodaté, empreinte, joint au devis) devient en revanche **nécessaire** avec E10.10b : un portail client doit montrer le document **envoyé**, pas un re-rendu de données vivantes. Tant que le devis `sent` est immuable, le re-rendu reste fidèle — c'est cela qui rend le report tenable, pas une approximation. En attendant, l'entrée d'audit `sent` porte un `quote_snapshot` complet : la seule preuve de ce qui a été transmis.

**Décisions de contrat prises faute de précision, à remonter à Arnaud si elles ne conviennent pas.**

| # | Point | Décision et raison |
|---|---|---|
| 1 | Forme de la remise globale | **Prix cible OU taux, exclusifs** (`target_net_total` XOR `global_discount_rate`), miroir exact d'E10.9 (`sale_price` XOR `margin_rate`) ; le montant de remise et le taux effectif sont **déduits** dans `totals`. L'écriture d'origine est **conservée** plutôt que normalisée, parce que les deux formes ne se comportent pas pareil quand une ligne change ensuite : un **taux suit** le devis, un **prix cible reste fixe**. C'est la différence qui doit guider le choix du commercial — la normaliser la lui retirerait. |
| 2 | Remise globale > sous-total | **Acceptée sans plafond ni alerte** : c'est une majoration, pas une anomalie (correction n° 2 d'Arnaud). `totals.global_discount` est **signé**, comme `QuoteLine.discount_rate`. Le code `global_discount_exceeds_lines_total` de la première rédaction n'existe plus — la situation qu'il décrivait était un artefact de la forme « montant de remise », abandonnée. |
| 3 | Remise globale négative | **Autorisée, sans borne** du côté majoration. Une seule borne subsiste, à `"1.0000"` côté remise : au-delà de 100 %, le devis paierait le client. Ce n'est pas une borne de prudence, c'est le point où la grandeur cesse d'avoir un sens — même motif que `MoneyNonNegative` sur un prix client. |
| 4 | Où se pose `show_discounts` | **Les deux** : `updateQuote` tant que le devis est brouillon, et une dernière fois dans le corps de `sendQuote`. Sur un **renvoi**, une valeur divergente est refusée en 422 `quote.resend_immutable` : deux clients détiendraient sinon deux documents différents portant le même numéro. |
| 5 | Ce que la duplication reprend | Toutes les lignes **avec leur geste commercial** (`sale_price`, remise, écart de marge) — les prix ne sont **pas** re-résolus contre les règles du jour. Une copie qui repartirait du tarif public tromperait le commercial, qui croit reprendre son offre. Reprend aussi `show_discounts`, `vat_rate` et la remise globale sous sa forme d'origine ; **jamais** le numéro (frais), le statut (toujours `draft`), ni `sent_at`/`last_sent_at`/`sent_by`. **`valid_until` est remise à `null`** (décision revue après le point 9 : une copie faite six mois plus tard hériterait sinon d'une date déjà passée et partirait périmée ; à `null`, elle sera recalculée à l'envoi depuis le réglage tenant). |
| 6 | Traçabilité de la duplication | **Dans les deux sens, sans doublon** : la copie porte `source_quote_id`, le journal d'entête de l'**original** reçoit une entrée `duplicated` portant l'identifiant de la copie. |
| 7 | Renvoi d'un devis déjà `sent` | **Autorisé**, même opération. Le contenu ne bouge pas (ni statut, ni lignes, ni remise, ni `sent_at`, ni `valid_until`) ; seul `last_sent_at` avance, une entrée d'audit `resent` est écrite et `quote.sent` est réémis avec `is_resend: true`. Refus (409 `quote.send_forbidden_status`) réservé à `accepted`/`rejected`/`converted`, qu'aucune opération ne produit aujourd'hui — la porte est fermée pour ne pas léguer à la story qui les introduira une décision qu'elle n'aurait pas prise. |
| 8 | Devis vide | Envoi refusé en 422 `quote.send_requires_lines`. Un devis sans ligne n'est pas une offre. |
| 9 | Validité par défaut | Réglage **tenant** `default_validity_days`, dans une **nouvelle ressource E10** `/commercial-settings` (voir ci-dessous). Appliqué **à l'envoi**, pas à la création, et **uniquement** si `valid_until` est encore `null` : une validité qui courrait depuis la rédaction ferait partir un brouillon vieux de trois semaines à moitié périmé. Défaut `null` = pas de validité — la façade n'invente pas « 30 jours » à la place d'une décision commerciale que personne n'a prise. Modifier le réglage ne recalcule **aucun** devis existant. |
| 10 | TVA | **Aucun nouveau réglage fiscal.** `tenants.tax_regime` existe depuis `20260511_02_R0` et sert déjà les commandes ; le module le **lit** (schéma `TaxRegime`, taux `metropole_fr` 20 % / `dom_tom` 8,5 % / les trois autres 0 %). `QuoteTotals` gagne `vat_rate`, `vat_regime`, `vat_amount`, `total_incl_tax` ; `net_total` reste le HT. Surcharge **par devis** (`Quote.vat_rate`) pour le cas qu'un régime de tenant ne couvre pas (client à l'export chez un tenant en métropole). **Pas** de régime fiscal par client (E10.4) : ce serait engager des mentions légales par régime que personne n'a demandées. |

**Où vit le réglage de validité, et pourquoi pas ailleurs (point 9).** Nouvelle ressource singleton `/commercial-settings` (`GET` ouvert aux membres, `PATCH` gardé par `can_manage_pricing`), table dédiée à colonnes nommées — pas un sac jsonb. Les trois domiciles candidats ont été écartés pour des raisons distinctes :

| Candidat | Pourquoi non |
|---|---|
| `userPreferences.default_delivery_zone` (module `session`) — le précédent qu'on m'avait signalé | C'est une préférence **d'utilisateur** (`userPreferencesSchema`, défaut codé en dur `'FR-75'` dans `session-service.ts`), pas un réglage de tenant. Deux commerciaux du même espace y auraient deux politiques de validité différentes. Ce n'était pas le précédent qu'il paraissait être. |
| `updateTenantSettings` (façade **historique**) | Gouverne l'identité du tenant (nom, slug, plan). Aucun **nouvel** endpoint n'est admis sur cette façade (dérogation R5, §8), et elle n'a ni enveloppe `{data, meta}`, ni `ETag`, ni journal. |
| `tenants.settings` (jsonb) | Sac libre, sans type ni garde : y loger une politique commerciale la rendrait inauditable et invérifiable. |

Cette ressource est par ailleurs le domicile que le sprint attend déjà pour le **seuil d'alerte de remise**, qu'E10.9 a laissé en constante avec la mention « rendre le seuil configurable reste à faire, et ce sera l'usage naturel de `can_manage_pricing` » (`QuoteLineWarning.threshold`). Il n'est **pas** livré ici, mais la forme est posée pour l'accueillir sans créer une seconde ressource de réglages.

**Impact du 06/09 sur le dimensionnement — à connaître avant de lancer dev-story.** Les points 9 et 10 sont un **ajout de périmètre** postérieur au cadrage initial. Le point 10 coûte peu (lecture d'une colonne existante + 4 champs dérivés dans `totals`) ; le point 9 ajoute une **ressource complète** (table, `GET`, `PATCH` avec `ETag`, garde de capability, tests). Si le lot doit être réduit, l'ordre de retrait est : (1) `listQuoteHeaderAuditEntries` — le journal serait toujours **écrit**, seule sa lecture attendrait ; (2) `/commercial-settings`, `valid_until` restant alors purement manuelle. Ni la remise globale, ni l'envoi/renvoi, ni la duplication, ni la garde de statut ne peuvent être retirés sans rouvrir le piège décrit plus haut.

**Deux écarts de forme assumés, à arbitrer en revue.**

1. **`If-Match` exigé sur un POST** (`sendQuote`) — seul cas du contrat. Le socle ne rend la précondition obligatoire que sur `PATCH`/`PUT` (`concurrencyGuarded`, `gescom-middleware.ts`) ; elle sera donc portée par le **service**, qui lira `context.ifMatch` (déjà transmis, `readIfMatch(request, false)` renvoie la valeur quand elle est présente) et lèvera les mêmes 428/409 partagés. Raison de fond : c'est l'opération qui remet un document à un tiers — transmettre la version qu'un collègue vient de modifier ne se rattrape pas, alors qu'un `PATCH` écrasé se refait. *Chemin de mise en conformité si un second POST en a besoin :* rendre `concurrencyGuarded` déclaratif dans `defineGescomRoute()` plutôt que dérivé de la méthode.
2. **Garde `can_manage_pricing` sur `listQuoteHeaderAuditEntries` et `updateCommercialSettings`** — troisième et quatrième sites d'application du même droit, par le **même** mécanisme applicatif (`actorHasCapability()`, E10.11 s5) plutôt que par le champ déclaratif prescrit par **s6** (§8.11). s6 vise l'ajout d'une capability *nouvelle* ; il s'agit ici du même droit. Introduire le champ déclaratif obligerait le middleware à connaître une notion d'autorisation qu'il n'a pas — c'est exactement le changement que s6 jugeait « plus large que cette story ». **À trancher explicitement en revue** : avec deux nouveaux sites au lieu d'un, l'argument « troisième call-site du même pattern » s'affaiblit ; si l'orchestrateur estime que s6 se déclenche, le faire ici plutôt que de le reporter une fois de plus.

**Un point d'implémentation qui n'est pas qu'un détail (point 10).** La correspondance régime → taux vit aujourd'hui dans `src/modules/orders/ui/helpers/tax.ts`, c'est-à-dire **dans le navigateur**. Pour E10 elle doit être résolue **côté serveur** : un taux de TVA calculé dans une UI est exactement le contrôle métier que `.claude/rules/frontend.md` interdit, et `QuoteTotals` serait sinon incalculable par la façade. La version historique reste en place pour les commandes (dérogation R5, existant non touché) ; dev-story ne doit pas l'importer depuis le module commercial-quotes, mais porter la table de taux au serveur — **avec les mêmes valeurs**, qui sont une donnée légale et non un réglage.

**Gap découvert en écrivant ce contrat, antérieur à E10.10 — à corriger dans ce lot.** `OUTBOX_EVENT_NAMES` (`src/modules/_shared/api/contracts.ts`) et `OUTBOX_EVENT_VERSIONS` (`outbox.ts`) **ne contiennent pas `quote_line.changed`**, pourtant déclaré comme webhook par E10.9 et décrit avec sa charge utile `QuoteLineChangedPayload`. Aucun code ne peut donc émettre cet événement — et rien ne l'a signalé, parce que `CONTRACT_ALIGNMENT` (§2.1) n'attrape que la direction **Zod → YAML** : une valeur ajoutée au contrat sans l'être côté Zod compile parfaitement. Le contrat promet un événement que le produit ne sait pas nommer. `quote.sent` tomberait dans le même trou : la story doit ajouter **les deux** aux deux registres, et le lint du contrat gagnerait à vérifier la direction manquante (chaque valeur de `EventName` au contrat existe dans `OUTBOX_EVENT_NAMES`).

**État des gates à la remise du contrat.** `pnpm test:architecture`, `pnpm gen:api:check` et `pnpm typecheck` passent. `pnpm test:contract` rend **7 échecs attendus**, tous de la même cause unique : `Quote`/`QuoteDetail` exigent désormais `source_quote_id`, `global_discount_rate`, `target_net_total`, `vat_rate`, `totals`, `warnings`, `sent_at`, `last_sent_at`, `sent_by`, que le service et son faux ne produisent pas encore. Les trois suites de niveau document (`openapi-document`, `gescom-routes`, `shared-components` — nommage CA3, scopes, capabilities, versionnement CA13) passent. Même situation, volontaire et déjà pratiquée, que le contrat d'E10.9 (commit `1671973`, « 6 échecs attendus […] à fermer par le dev-story dans le même lot, pas une régression »).

### 8.12bis E10.10a — implémentation (dev-story), ce qui a été livré et ce qui reste ouvert

**Les 7 échecs attendus sont fermés.** `Quote`/`QuoteDetail` portent désormais `source_quote_id`, `global_discount_rate`, `target_net_total`, `vat_rate`, `totals` (`QuoteTotals`), `warnings` (`QuoteWarning[]`), `sent_at`, `last_sent_at`, `sent_by` — calculés par le SERVEUR (`computeQuoteTotals()`/`computeQuoteWarnings()`, `src/modules/commercial-quotes/application/quote-totals.ts`, arithmétique entière en centimes/dix-millièmes, même discipline que `quote-line-pricing.ts`, jamais de flottant).

**Décisions prises en écrivant le code, faute d'un dernier degré de précision dans le contrat :**

| # | Point | Décision |
|---|---|---|
| 1 | Écart de forme n°1 (§8.12, `If-Match` sur `sendQuote`) | Tranché en faveur d'un appel **répété** à `readIfMatch(context.request, true)` dans la ROUTE (pas le service) — la même fonction que le socle utilise déjà pour un PATCH, rejouée avec `required: true`. Produit exactement les mêmes codes 428/400 sans dupliquer leur construction. `context.ifMatch` (déjà lu avec `required: false` par le socle) n'est pas utilisé directement pour cette raison. |
| 2 | Écart de forme n°2 (§8.12, garde `can_manage_pricing` sur deux nouveaux sites) | Tranché en faveur du mécanisme applicatif existant (`actorHasCapability()` + `roleRequired()`), **pas** du champ déclaratif de s6 — cohérent avec la réserve de l'architecte, mais **à revalider en `qa-review`** : c'est elle qui doit dire si quatre sites suffisent à déclencher s6. |
| 3 | `sendQuote`/`duplicateQuote` : SQL ou TypeScript ? | **SQL** (`api_send_commercial_quote`, `api_duplicate_commercial_quote`, migration `20260906160000`), même famille que `api_create_commercial_quote_from_project_items`/`api_reorder_commercial_quote_lines` (E10.3/E10.9) : ce sont des transitions **structurelles** multi-lignes (statut, audit, copie de lignes), pas un calcul de prix — PostgREST n'offrant pas de transaction multi-requêtes, une fonction plpgsql reste le seul moyen de les rendre atomiques. Aucune ligne de prix n'est recalculée dans ces fonctions (E10.8 reste gelée) : les lignes copiées le sont à l'identique, colonne par colonne. |
| 4 | `api_send_commercial_quote` : `security invoker` ou `definer` ? | **`definer`**, en écart du premier réflexe (« aucune table n'a de RLS plus stricte qu'un membre ») : la fonction écrit **directement** une entrée `sent`/`resent` dans `commercial_quote_header_audit`, fermée en `INSERT` à `authenticated` (append-only). En `invoker`, cette écriture aurait échoué pour l'appelant réel — seul le trigger générique `updated` (lui-même `definer`) s'en serait sorti, laissant `sent`/`resent` introuvables. La fonction vérifie donc elle-même l'appartenance au tenant, comme `api_duplicate_commercial_quote`. |
| 5 | `commercial_settings`, où vit le port applicatif ? | **Nouveau module** `src/modules/commercial-settings/`, pas une extension de `commercial-quotes` : ressource et cycle de vie propres (GET ouvert à tout membre, PATCH gardé). `default_validity_days` est lu **directement en SQL** par `api_send_commercial_quote` (même transaction que la transition) — **aucun port TypeScript croisé** entre les deux modules n'a été créé pour cette lecture, décision documentée dans `commercial-settings-repository.ts` pour qu'une story future ne le recrée pas par réflexe. |
| 6 | Table TVA régime → taux | Portée **dans** `quote-totals.ts` (module `commercial-quotes`), pas dans un module `tax` séparé : c'est le seul consommateur aujourd'hui, et la valeur légale est déjà commentée comme non réutilisable telle quelle ailleurs sans revue (mêmes 5 valeurs que `src/modules/orders/ui/helpers/tax.ts`, dupliquées consciemment plutôt qu'importées depuis un module `ui/`). |
| 7 | `updateQuote`, garde de statut posée en base ou en application ? | **Application** (filtre `.eq('status','draft')` sur l'`UPDATE`, comme `deleteQuote` déjà établi par E10.3/E10.9), pas un nouveau trigger BEFORE dédié. Cohérent avec le précédent déjà accepté sur `deleteQuote` ; un contournement par appel PostgREST direct reste théoriquement possible sur les DEUX opérations (dette préexistante, pas introduite par ce lot — à re-signaler si `qa-review` juge que ça doit changer maintenant que la garde devient réellement observable). |

**Gap `OUTBOX_EVENT_NAMES`/`OUTBOX_EVENT_VERSIONS`** (signalé par l'architecte, §8.12) : **fermé**. `quote_line.changed` et `quote.sent` ajoutés aux deux registres (`src/modules/_shared/api/contracts.ts`, `src/modules/_shared/application/outbox.ts`). Le lint symétrique (YAML → Zod) suggéré par l'architecte n'a **pas** été ajouté dans ce lot — jugé plus large que cette story (il toucherait le harnais de lint partagé par tous les modules E10.x, pas seulement celui-ci) ; **tracé comme dette**, à ouvrir explicitement si un futur contrat rouvre le même trou.

**`quote_line.changed` reste un événement JAMAIS ÉMIS.** Corriger le registre ne fait que rendre le NOM valide côté Zod ; aucun code de ce lot ni d'E10.9 n'appelle `outbox.publish({name: 'quote_line.changed', ...})`. Le contrat continue donc de décrire un webhook qu'aucun abonné ne recevra jamais avant qu'une story implémente réellement cette émission — **dette distincte**, non résolue ici (hors périmètre E10.10a, qui n'a fait que réparer le registre pour que le nom compile).

**UI — NON livrée dans ce lot, décision à faire remonter à Arnaud.** Aucun composant `src/modules/commercial-quotes/ui/` n'a été modifié : ni bouton d'envoi/renvoi/duplication sur `QuoteEditorPage`, ni saisie de la remise globale, ni affichage de `default_validity_days`/des totaux serveur. Choix fait faute de budget dans ce lot, **pas** une clause du contrat (rien dans `docs/api/CONVENTIONS.md` §8.12 ni dans le brief de la story ne dispense l'UI, à la différence du point 1b « impression », explicitement laissé à discrétion). L'API est entièrement fonctionnelle et testée sans elle ; l'écran ne l'est pas. À planifier comme un lot UI dédié.

**UI — livrée dans un lot séparé (`dev-story`, branche `feat/gescom-e10-4-entite-client`, après le round `qa-review` 4 ci-dessus).** `QuoteEditorPage` (`src/modules/commercial-quotes/ui/workspace/QuoteEditorPage.tsx`) porte désormais les huit points du brief UI : bouton Envoyer/Renvoyer avec confirmation de `show_discounts` (verrouillée en lecture seule sur un renvoi, jamais transmise dans le corps de `sendQuote` dans ce cas — évite tout risque de divergence de forme avec `quote.resend_immutable`) ; bouton Dupliquer disponible quel que soit le statut, avec navigation vers la copie et badge de traçabilité « Dupliqué depuis DEV-XXXX » (second appel `getQuote(source_quote_id)` pour résoudre le numéro, l'API ne le portant pas — dégrade sur l'UUID brut si le devis source a été supprimé depuis, cohérent avec la dette u4/B6 déjà actée) ; remise globale en taux XOR prix cible, deux champs toujours visibles, commit au blur, remise déduite affichée en lecture seule (exact miroir du geste ligne d'E10.9) ; encart de totaux serveur (`QuoteTotals`), aucun recalcul client ; TVA — taux/régime effectifs affichés, surcharge par devis éditable en brouillon, mention légale déduite du régime (`franchise_tva` → « TVA non applicable, art. 293 B du CGI », `export_eu`/`export_world` → mention d'autoliquidation, aucune mention pour `metropole_fr`/`dom_tom` ou une surcharge explicite) ; réglage `default_validity_days` exposé dans `PricingRulesPage` (panneau `DefaultValidityDaysPanel`, même patron que le panneau de marge standard E10.6) et `valid_until` affiché/éditable avec bandeau `validity_expired` non bloquant sur l'écran devis ; garde de statut : toute l'entête ET les lignes basculent en lecture seule dès que `status !== 'draft'`, un bandeau dédié met en avant Dupliquer ; journal d'entête (`listQuoteHeaderAuditEntries`) accessible par un onglet distinct du journal des lignes, rendu uniquement si `hasCapability('can_manage_pricing') === true` (même mécanisme que la garde de route d'E10.11/E10.6, pas une nouvelle convention).

Décisions prises faute d'un dernier degré de précision, à signaler si elles ne conviennent pas :

| # | Point | Décision |
|---|---|---|
| 1 | Conversion pourcentage ↔ taux pour la remise globale | **Dupliquée** depuis `src/modules/pricing/ui/workspace/rate-format.ts` dans `src/modules/commercial-quotes/ui/helpers/rate-format.ts`, plutôt qu'importée : cette paire de fonctions n'est pas publiée par `pricing/ui/index.ts`, et `tests/architecture/modular-ui-boundaries.test.ts` n'autorise un import inter-module côté UI que vers une entrée publique. La version dupliquée diffère en un point : `percentToRate()` accepte un **signe** (majoration, §8.12 décision 3), contrairement à `toRateString()` de `pricing` qui la rejette (pas de majoration au niveau d'une règle de prix). Même précédent déjà assumé côté serveur pour la table de TVA (`quote-totals.ts`, décision 6 ci-dessus). |
| 2 | Surcharge `vat_rate` par devis | **Dans le périmètre**, contrairement à l'hypothèse par défaut évoquée dans le brief : un champ dédié (« Surcharge (%) »), éditable seulement en `draft`, à côté du taux effectif affiché en lecture seule. |
| 3 | Où poser la remise globale/TVA dans le formulaire d'entête | **Étendu le même `<section>`** que `show_discounts`/`valid_until` (E10.3) plutôt que créé un nouvel écran ou une nouvelle carte séparée — un devis reste un objet unique. Chaque champ bascule individuellement en lecture seule selon `isDraft`, plutôt que de masquer toute la section (point 3/7 du brief : le contrôle doit rester **visible**, pas disparaître, sur un devis `sent`). |
| 4 | Panneau de validité par défaut | Ajouté à `PricingRulesPage` (E10.6/E10.11) comme demandé, **pas** un nouvel écran de réglages commerciaux séparé — cohérent avec le fait que `/commercial-settings` n'a pas de surface dédiée à ce jour et que le brief renvoie explicitement vers « l'écran des paramètres commerciaux ». |
| 5 | Confirmation d'envoi | Panneau **inline** (même patron que `addLineDrawer`, pas une modale Radix `AlertDialog`) : la confirmation doit pouvoir rester ouverte et réafficher une erreur métier (422 `quote.send_requires_lines`/`quote.resend_immutable`, 409 `quote.send_forbidden_status`) sans se fermer automatiquement, ce que le flux `AlertDialogAction`/`onOpenChange` déjà utilisé par `QuotesPage` (suppression) ne permet pas nativement. |
| 6 | Journal d'entête : `status_forced` | Rendu en évidence (texte en gras, couleur d'alerte) dans la liste plutôt que dans une ligne neutre — cohérent avec §8.12ter (« une entrée à lire comme une alerte »), sans composant de bandeau dédié faute de précision sur son urgence opérationnelle réelle. |

Ce qui **n'a pas** été fait, par manque de précision ou hors périmètre explicite :

- Aucun test de composant React (`@testing-library/react`) n'a été ajouté pour `QuoteEditorPage`/`PricingRulesPage` : aucun des deux écrans n'en avait avant ce lot, et il n'existe aucun harnais de rendu de composant déjà en place pour ce module (précédent absent). Les deux fonctions pures nouvellement introduites (`percentToRate`/`rateToPercent`, `vatLegalMention`) sont testées unitairement (`tests/modules/commercial-quotes/ui-helpers.test.ts`, 12 cas), même discipline que `quote-line-pricing.test.ts`/`quote-totals.test.ts`. **Dette** : un test de composant couvrant le flux golden path (envoi → statut `sent` → contrôles lecture seule → duplication) reste à écrire si un harnais RTL se généralise sur d'autres écrans du sprint.
- Vérification visuelle du flux golden path en navigateur : **non faite**. Cet environnement de session n'a pas d'outil de contrôle de navigateur (pas de MCP Chrome/Playwright disponible) ; `pnpm build` a été exécuté avec succès (aucune erreur de bundling/JSX) et `pnpm typecheck`/`pnpm test:architecture`/`npx vitest run` passent, mais aucun de ces trois ne constitue une preuve de rendu correct à l'écran. À vérifier manuellement avant merge, ou via un run `qa-review` équipé.
- `data-testid` posés sans confrontation aux Hints DOM d'un cas de test Notion pour cette sous-story (aucun cahier P13 dédié à E10.10a n'a été fourni à ce lot) : les noms suivent la convention `<scope>-<element>[-<modifier>]` déjà établie et restent cohérents avec ceux d'E10.9 sur le même écran, mais pourraient nécessiter un renommage une fois le cahier de test rédigé par le `scribe`.

**État des gates après implémentation** (voir rapport de fin de story pour le détail complet) : `pnpm typecheck`, `pnpm test:contract` (0 échec, les 7 attendus fermés), `pnpm test:architecture`, `pnpm gen:api:check` passent tous. `npx vitest run` : 1569 passés, 3 échecs pré-existants et sans rapport (`tests/storage/product_mockups_isolation.test.ts`, bucket Storage absent en local), 36 skip. `pnpm test:storefront:sql` reste injouable (Docker absent du poste) ; le cas `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` a été écrit et relu ligne à ligne avec la même rigueur que celui d'E10.11, jamais exécuté.

**Corrigé au round `qa-review` 1 (bloquants B1/B2/B3, réserves R1/R2/R7)**, sur les commits qui suivent `badb682`/`d90c0b6` ci-dessus.

- **Bloquant B1.** `rateSchema` (signé, sans borne basse) était utilisé pour `Quote.vat_rate`/`QuoteDetail.vat_rate`/`UpdateQuoteCommand.vat_rate`/`QuoteTotals.vat_rate`, sans CHECK en base sur `commercial_quotes.vat_rate`. `PATCH /quotes/{id}` avec `vat_rate: "-0.2000"` était accepté et écrit ; `computeQuoteTotals()` appelle ensuite `formatCentsToMoneyNonNegative()` sur `vat_amount`, qui LÈVE sur une valeur négative — sur le chemin de `findById`/`findDetailById`/`list()`, donc de TOUTE lecture du devis touché et de la liste paginée qui l'inclut. Pire : `updateQuote` appelle `getSummary()` AVANT d'appliquer le patch, rendant impossible de reposer `vat_rate` à `null` par l'API.
  - **Correction, aux 3 niveaux demandés.** (1) Zod : les 4 usages remplacés par `nonNegativeRateSchema`, réutilisé **tel quel** depuis `src/modules/pricing/api/contracts.ts` (E10.6) plutôt que dupliqué — même précédent que `PriceRule.value` côté OpenAPI (`$ref: '#/components/schemas/Rate'` conservé, la borne non-négative n'existe que côté Zod et dans la description ; **aucune modification d'`openapi/`**, hors périmètre `dev-story`). (2) Migration : `commercial_quotes_vat_rate_non_negative check (vat_rate is null or vat_rate >= 0)` ajoutée **directement** dans `20260906160000_gescom_e10_10a_send_duplicate_global_discount.sql` — non déployée (migration de ce lot, jamais poussée sur le projet partagé ; vérification par raisonnement seulement, `supabase migration list --linked` a échoué en session faute de PAT valide, cf. dette ci-dessous). (3) Défense en profondeur : `computeQuoteTotals()` (`quote-totals.ts`) CLAMPE désormais un `vat_rate` négatif à zéro avant de calculer `vat_amount`/`total_incl_tax`, au lieu de lever — une lecture ne peut plus jamais 500 à cause d'une donnée de TVA aberrante, même si elle se retrouvait en base par un autre biais que l'API. Testé : `tests/modules/commercial-quotes/quote-totals.test.ts` (clamp unitaire), `tests/contract/commercial-quotes.contract.test.ts` (422 à l'écriture, lecture/liste toujours 200 ensuite), `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` scénario 6 (CHECK en base).
  - **Dette résiduelle (non bloquante).** La description OpenAPI de `vat_rate` (les 3 sites côté `Quote`/`QuoteDetail`/`UpdateQuoteCommand`) ne documente pas encore explicitement « toujours positif ou nul », contrairement à `PriceRule.value`. Écart de **texte** seulement (le `$ref: Rate` reste inchangé, aucune incohérence de schéma) : à signaler à l'agent `architecte` pour alignement rédactionnel, pas un blocage fonctionnel.
- **Bloquant B2.** `subtotalsByQuoteId()` (`commercial-quotes-repository.ts`) faisait un `select quote_id, sale_price ... in (quoteIds)` SANS `limit`/`order`. Au-delà de `max_rows` (1000, PostgREST), les lignes des devis les plus chargés d'une page étaient tronquées SANS ERREUR et, sans `order by`, de façon non déterministe d'un appel à l'autre — `lines_subtotal`/`net_total`/`global_discount`/`vat_amount`/`total_incl_tax` devenaient faux et instables, sans aucun signal.
  - **Correction.** Nouvelle fonction `api_commercial_quote_line_subtotals(p_tenant_id, p_quote_ids)` (`security invoker`, même migration), qui AGRÈGE EN BASE (`group by quote_id`) : le nombre de lignes retournées à l'appelant est borné par le nombre de DEVIS demandés (une page), jamais par leur nombre de lignes — `max_rows` ne peut plus être atteint dans la plage de pagination du contrat. `subtotalsByQuoteId()` délègue désormais entièrement à cette fonction via `.rpc()`, a gagné un paramètre `tenantId` (défense en profondeur, cohérent avec le filtre déjà appliqué par la fonction). Testé : `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` scénario 9 (agrégation correcte + isolation inter-tenant, y compris un `p_tenant_id` usurpé rejeté par la RLS `security invoker` indépendamment du paramètre fourni).
- **Bloquant B3.** `commercial_quotes_write` (RLS, `20260901000600`) autorise tout `admin`/`member` du tenant à UPDATE n'importe quelle colonne de `commercial_quotes` SANS condition de statut — contrairement aux LIGNES, déjà gardées en base par `commercial_quote_lines_require_draft_quote` (E10.9). Un appel PostgREST direct pouvait redonner silencieusement l'état `draft` à un devis `sent` (rouvrant ses lignes et sa remise globale), sans laisser aucune trace : le trigger d'audit d'entête ne trace que 5 champs, `status` en est exclu par contrat (`QuoteAuditField` documente explicitement son absence, « une transition a son action propre »). Second point : `sent_at = now()` était écrit sans condition dans la branche `draft` de `api_send_commercial_quote`, l'invariant « figé au premier envoi » ne tenant que parce qu'aucun chemin d'API ne permettait de repasser un devis en `draft`.
  - **Correction (volets 1 et 2, faits).** Nouveau trigger `BEFORE UPDATE` `commercial_quotes_require_draft_before_write()` (section 2ter de la migration) : refuse toute modification quand `old.status <> 'draft'`, sauf échappatoire `magrit.quote_transition` (`set_config(..., true)`, même mécanisme que `magrit.change_set_id`), posée par `api_send_commercial_quote` juste avant ses deux branches d'UPDATE. `api_duplicate_commercial_quote` n'a besoin d'aucune échappatoire : elle n'UPDATE jamais le devis ORIGINAL. `sent_at = coalesce(v_quote.sent_at, now())` dans la branche `draft` (défense en profondeur, une ligne). Testé : `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` scénario 3bis (dégel direct rejeté, modification d'une autre colonne sans toucher au statut également rejetée, valeurs inchangées vérifiées).
  - **Volet 3, escaladé à l'agent `architecte` (round 1), fermé au round `qa-review` 1 suite.** Ajouter `status` aux champs tracés par `commercial_quotes_write_header_audit()` exigeait d'étendre `openapi/magrit-core.v1.yaml`, hors périmètre `dev-story` — voir l'arbitrage de l'architecte, §8.12ter (nouvelle action `status_forced`, `QuoteAuditField` reste fermée à 5 champs). L'analyse de risque résiduel du round 1 (« un dégel illégitime est rejeté avec une exception ») était exacte **sur le dégel**, mais l'architecte a trouvé un second trou en réinstruisant la garde elle-même : `commercial_quotes_require_draft_before_write()` ne regardait que `old.status`, et laissait donc passer un changement de `status` **DEPUIS** `draft` (`PATCH {"status":"accepted"}` sur un devis encore `draft`) — un jeton de membre ordinaire suffisant, aucun privilège élevé requis. Fermé ci-dessous.
  - **Correction (volet 3, fait).** (1) `commercial_quotes_require_draft_before_write()` : la condition de sortie anticipée devient `new.status is not distinct from old.status and old.status = 'draft'` (au lieu de `old.status = 'draft'` seul) — un devis `draft` reste modifiable librement tant que son `status` ne change pas, mais tout changement de `status`, DEPUIS `draft` comme depuis tout autre état, exige désormais systématiquement l'échappatoire `magrit.quote_transition`. Sans risque pour le chemin légitime : `api_send_commercial_quote` pose cette échappatoire avant ses DEUX branches (`draft → sent` et `sent → sent`), `api_duplicate_commercial_quote` n'`UPDATE` jamais l'original. (2) Sixième branche dans `commercial_quotes_write_header_audit()` (AFTER UPDATE) : si `new.status is distinct from old.status` et que l'échappatoire n'est pas posée, insertion d'une entrée `status_forced` (`field`/`quote_snapshot` `null`, `previous_value`/`new_value` = les deux `QuoteStatus`, `actor_id` nullable). Condition strictement disjointe des branches `sent`/`resent` (qui posent toujours l'échappatoire) : aucun doublon possible sur un envoi/renvoi normal. (3) `action in (...)` et `commercial_quote_header_audit_shape` : `status_forced` ajouté à l'énumération et à la branche de forme `field is null and quote_snapshot is null` (celle de `resent`/`duplicated`). (4) `quoteAuditActionSchema` (Zod) : valeur ajoutée. Tout dans la migration `20260906160000` (toujours **non déployée**, dette u1 inchangée) et `src/modules/commercial-quotes/api/contracts.ts`. Testé : `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` scénario 3ter — (a) le `PATCH` direct depuis `draft` est désormais REJETÉ EN AMONT par la garde étendue et ne produit **aucune** entrée `status_forced` (bloquer vaut mieux que constater, la garde couvre ce cas à elle seule) ; (b) le scénario RÉSIDUEL que `status_forced` vise réellement — trigger de prévention désactivé EXPLICITEMENT (simulant une session privilégiée qui le contournerait), le trigger d'audit produit alors l'entrée attendue (`previous_value`/`new_value` = `draft`/`accepted`, `field`/`quote_snapshot` `null`) ; (c) non-régression explicite — l'envoi ET le renvoi normaux (scénarios 2/3) ne produisent jamais `status_forced`.
- **Réserve R1.** `commercial-settings-routes.ts` (`PATCH`) lisait la ressource courante (`assertPrecondition`/ETag) AVANT la garde `can_manage_pricing` — même réserve que R3 d'E10.11 round 2. Corrigé de la même façon : `CommercialSettingsService.assertCanManagePricing()` rendue publique (appelée en interne par `update()` en défense en profondeur), la route l'appelle désormais explicitement EN PREMIER, avant la lecture qui alimente l'ETag.
- **Réserve R2.** Ajouté `tests/contract/commercial-quotes.contract.test.ts` : « `valid_until` posée manuellement n'est JAMAIS recalculée à l'envoi, même si `default_validity_days` est réglé » — `PATCH valid_until` puis `default_validity_days = 30` puis `sendQuote`, `sent.valid_until` reste la valeur manuelle.
- **Réserve R7.** (a) En-tête de `20260906160000...sql` corrigé : annonçait `api_send_commercial_quote` en `security invoker`, la fonction est réellement `security definer` (cohérent avec §8.12bis décision 4, déjà correcte dans le corps de la migration et dans `commercial-quotes-repository.ts`). (b) Trois occurrences de la migration inexistante `20260906000100` corrigées en `20260906160000` : `commercial-quotes-repository.ts:263` (devenu ~278 après l'édit B1/B2), `commercial-settings-repository.ts:5`, `src/modules/commercial-settings/application/commercial-settings-repository.ts:44`.
- **Réserves non traitées, par instruction explicite** : R3 (dette `If-Match` sur POST), R4 (garde `can_manage_pricing` non déclarative), R5 (`quote_line.changed` jamais émis), R6 (ETag dépendant de l'horloge), R8 (`actorHasCapability` ignore `actorId`) — dette acceptée, inchangée par ce round.

**Dette introduite ou confirmée par ce round.**

| Réf. | Trou | Chemin de mise en conformité |
|---|---|---|
| **u1** | Migration `20260906160000` (colonnes E10.10a, trigger d'immuabilité B3, CHECK B1, fonction d'agrégation B2) jamais déployée sur le projet Supabase partagé ni exécutée localement (Docker absent) — même famille que **t1** (§8.11). Le fichier `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` a été relu ligne à ligne mais jamais lancé. | `pnpm db:local:start && pnpm test:storefront:sql` sur un poste équipé Docker, puis `supabase db push --linked` avec un PAT valide. |
| **u2** | ~~`openapi/magrit-core.v1.yaml` : `QuoteAuditField`/`QuoteAuditAction` n'incluent pas `status`~~ — **tranché par l'architecte (§8.12ter) et implémenté côté `dev-story` au round `qa-review` 1 suite** (trigger de prévention étendu, sixième branche du trigger d'audit, `check` de forme, `quoteAuditActionSchema`, cas SQL 3ter). | Clos, sous réserve du déploiement — voir **u1** (même migration, jamais poussée). |
| **u3** | ~~Description OpenAPI de `vat_rate` (3 sites) ne documente pas « toujours positif ou nul »~~ — **fait** dans le même lot que §8.12ter : les trois sites (`Quote`, `QuoteDetail`, `UpdateQuoteCommand`) portent désormais la borne en toutes lettres, avec sa raison (`vat_amount` est un `MoneyNonNegative`, une TVA négative casserait toute *lecture*) et le motif du `$ref: Rate` conservé. `QuoteDetail.vat_rate`, qui n'avait aucune description, en a une. Aucun changement de schéma. | Clos. |
| **u4** | `commercial_quotes` n'a aucune garde de cohérence de tenant au niveau colonne (`customer_id`, `project_id`, et depuis le retrait de FK du round `qa-review` 3 — bloquant B6 — `source_quote_id` non plus) : un `PATCH` PostgREST direct sur un devis `draft` peut y écrire un id d'un autre tenant. Non exploitable en lecture aujourd'hui — vérifié par `qa-review` round 4 : aucune jointure sur `source_quote_id` nulle part dans le dépôt (TypeScript ou SQL), donc rien ne dérefférence la valeur écrite. Family de dette préexistante à E10.10a (`customer_id`/`project_id` datent d'E10.3), pas une régression de ce lot — le retrait de la FK sur `source_quote_id` a d'ailleurs *fermé* un oracle d'existence inter-tenant que la contrainte laissait ouvert (un `23503` distinguait un id existant d'un id inexistant, sans filtrer par tenant). | Trigger de cohérence de tenant sur ces colonnes, à poser quand une story rendra l'une d'elles réellement déréférençable (jointure, affichage, etc.). |

### 8.12ter E10.10a — traçabilité d'un changement de `status` au journal d'entête (arbitrage architecte, B3 volet 3)

**Ce qui était escaladé.** `qa-review` demandait qu'un changement de `status` non sanctionné laisse une trace ; `dev-story` ne pouvait pas le faire sans arbitrage, `QuoteAuditField` excluant `status` **explicitement et par contrat** et `QuoteAuditAction` n'ayant aucune valeur pour ce cas (dette **u2** ci-dessus).

**Décision — une nouvelle valeur d'action, `status_forced` ; `QuoteAuditField` reste fermée à ses 5 champs commerciaux.**

Le principe « une transition a son action propre » n'est pas amendé, il est **étendu** : un changement d'état a toujours une action, jamais une entrée « champ changé ». Sanctionné, il porte celle de sa transition (`sent` et son instantané, `resent`) ; posé hors façade, il porte `status_forced`, avec `previous_value`/`new_value` = les deux `QuoteStatus`, `field` et `quote_snapshot` à `null`.

Pourquoi pas `status` dans `QuoteAuditField`, qui aurait été plus économique :

| | Conséquence |
|---|---|
| `field: 'status'` sous l'action `updated` | Un envoi légitime produirait **deux** entrées pour un seul fait (un `updated`/`status` **et** un `sent`), et surtout : `updated` signifierait tantôt « un commercial a consenti une remise » — le cas ordinaire, celui pour lequel ce journal existe — tantôt « quelqu'un a écrit en base ». Un lecteur devrait connaître la liste des champs anormaux pour trier. La sémantique la plus chargée du journal serait celle qu'on lit le moins attentivement. |
| Action dédiée (**retenu**) | L'anomalie **se nomme**. Elle est filtrable (`action = 'status_forced'`), elle ne dilue pas `updated`, et elle laisse la porte ouverte : le jour où une story livre `accepted`/`rejected`/`converted`, chacune prend son action propre sans rien renégocier. |

**Nom : `status_forced`, pas `status_reset`.** `reset` ne décrit que le dégel `sent → draft`, qui n'est **pas** le seul cas — voir le trou ci-dessous. `forced` dit ce qui s'est réellement passé : un statut écrit directement, sans passer par une transition.

**Pourquoi formaliser une action qu'aucun chemin normal ne produit — et pourquoi ce n'est pas seulement de la défense en profondeur.** L'analyse de risque de `dev-story` (« une fois le volet 1 en place, un dégel illégitime est rejeté avec une exception ») est exacte **sur le dégel**, et c'est bien pour cela que le contrat dit noir sur blanc que bloquer vaut mieux que constater. Mais le trigger `commercial_quotes_require_draft_before_write()` ne regarde que `old.status` : il **laisse passer** un `PATCH` PostgREST direct `{"status":"accepted"}` (ou `"sent"`, ou `"converted"`) sur un devis **`draft`** — la policy `commercial_quotes_write` l'autorise à tout `admin`/`member`, le `check` de colonne accepte les 5 valeurs, et le trigger d'audit d'en-tête n'écrit rien puisque `status` n'est pas tracé. Aucun privilège élevé n'est requis : un jeton de membre ordinaire suffit.

Ce chemin est aujourd'hui **pire qu'avant le volet 1**, pas meilleur : le devis atterrit dans un état que personne n'a décidé, sans `sent_at`, sans instantané, sans auteur — **et il y est désormais figé**, le trigger d'immuabilité interdisant toute correction par l'API. `status_forced` n'est donc pas une action « qui ne peut jamais survenir » : c'est le cas **par défaut** du journal sur le statut, et il est atteignable dès aujourd'hui. Tant qu'elle n'apparaît jamais, elle prouve que tout changement d'état est passé par la façade ; le jour où elle apparaît, elle est en soi l'information à lire.

**Ce qui reste à implémenter (`dev-story`, aucune ligne écrite ici — contrat et documentation uniquement).**

1. **Prévention d'abord, et c'est le point le plus important.** Étendre `commercial_quotes_require_draft_before_write()` pour refuser aussi `new.status is distinct from old.status` **depuis `draft`**, hors échappatoire. Sans risque pour le chemin légitime : `api_send_commercial_quote` pose `magrit.quote_transition` **avant ses deux branches** (migration `20260906160000`, ligne ~469), et `api_create_commercial_quote_from_project_items` / `api_duplicate_commercial_quote` font des `INSERT`, jamais des `UPDATE` du devis concerné. À traiter au même niveau de gravité que B3 volet 1.
2. **Détection ensuite.** Sixième branche dans `commercial_quotes_write_header_audit()` : si `new.status is distinct from old.status` **et** que `magrit.quote_transition` n'est pas posé, insérer une entrée `status_forced` (`field` null, `quote_snapshot` null, `previous_value`/`new_value` = les deux statuts). La condition sur l'échappatoire est ce qui évite le doublon avec `sent`/`resent` sur le chemin légitime. `actor_id`/`actor_label` peuvent être `null` (`auth.uid()` absent hors session utilisateur) : le contrat l'autorise explicitement pour cette action, et c'est même une information à conserver.
3. `action in (...)` et `commercial_quote_header_audit_shape` : ajouter `status_forced` à la branche qui exige `field is null and quote_snapshot is null` (celle de `resent`/`duplicated`).
4. `quoteAuditActionSchema` (`src/modules/commercial-quotes/api/contracts.ts`) : ajouter la valeur. `COMMERCIAL_QUOTES_CONTRACT_ALIGNMENT` reste vert dans les deux sens (l'assertion est `Zod ⊆ contrat`).
5. `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` : deux scénarios — (a) `draft → accepted` en direct rejeté après le point 1 ; (b) l'entrée `status_forced` réellement écrite quand le changement atteint la ligne (à provoquer en désactivant temporairement le seul trigger de prévention, la table d'audit restant intacte). Et la non-régression qui compte : un envoi/renvoi normal ne produit **aucune** entrée `status_forced`.

**Point de méthode, à ne pas perdre.** L'escalade portait sur « comment tracer un dégel » ; le contrat l'a instruite en repartant de la garde elle-même, et c'est ce détour qui a trouvé le trou `draft → X` — que ni le volet 1 ni le volet 3 tels que formulés ne fermaient. Une demande de traçabilité mérite toujours qu'on redemande d'abord ce qui, exactement, reste possible.

**Implémenté (`dev-story`, round `qa-review` 1 suite).** Les 5 points ci-dessus sont faits, dans la même migration (`20260906160000`, toujours non déployée) et le même fichier de contrat Zod — détail complet en §8.12bis, « Volet 3 ». Résumé du test SQL (scénario 3ter) : (a) confirme que le point 1 rend le point 2 muet sur le cas ordinaire (la garde bloque, aucune entrée) ; (b) provoque le cas RÉSIDUEL en désactivant explicitement le seul trigger de prévention (le trigger d'audit restant actif) pour vérifier que `status_forced` est réellement écrite quand cette garde-là est elle-même contournée ; (c) non-régression sur l'envoi/le renvoi normaux.

**Corrigé au round `qa-review` 2 (bloquants B4/B5, nit N3).** Deux bloquants NOUVEAUX, introduits par le lot round 1 (pas préexistants) :

- **Bloquant B4.** `source_quote_id` (colonne ajoutée par ce lot) ne portait AUCUNE clause `on delete` (`no action` par défaut). Scénario entièrement nominal : un devis A `draft` dupliqué (`POST /quotes/{A}/duplicates`, autorisé depuis n'importe quel statut par contrat) produit une copie B avec `B.source_quote_id = A.id` ; supprimer ensuite A (`DELETE /api/v1/quotes/{A}`, A toujours `draft`, la garde CA6 passe) levait `23503` (violation de FK) — `remove()` (`commercial-quotes-repository.ts`) ne mappait ce Postgres brut vers aucune erreur de domaine, produisant un 500 `api.internal_error`. A devenait alors indéfiniment indélébile par toute API.
  - **Correction.** `source_quote_id uuid references public.commercial_quotes(id) on delete set null` — cohérent avec le commentaire de colonne du lot lui-même (filiation informative, aucune synchronisation) : supprimer l'original ne doit ni bloquer ni entraîner la copie, seulement lui faire perdre la trace de son origine. Corrigé directement dans `20260906160000...sql` (non déployée, dette **u1** inchangée — « une migration déployée ne change jamais » ne s'applique pas ici). Fake de contrat (`commercial-quotes-repository.fake.ts`, `remove()`) mis en cohérence : nulle aussi `source_quote_id` des copies en mémoire, pour ne pas diverger de la contrainte réelle. Testé : `tests/contract/commercial-quotes.contract.test.ts` (« B4 — supprimer un devis A DEJA dupliqué réussit, la copie perd son `source_quote_id` »), `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` scénario 5bis (duplication puis suppression directe de l'original, `source_quote_id` de la copie vérifié `NULL` après coup).
- **Bloquant B5.** Le trigger d'immuabilité posé au round 1 (`commercial_quotes_require_draft_before_write`) était `BEFORE UPDATE` **seulement**. `commercial_quotes_write` (RLS) est un `for all` sans condition de statut, qui couvre donc aussi le `DELETE` : un `DELETE` PostgREST direct sur un devis `sent` par un membre ordinaire n'était bloqué NULLE PART en base, et emportait par cascade `commercial_quote_lines` **et les deux journaux append-only** — `commercial_quote_line_audit` et `commercial_quote_header_audit` (donc l'entrée `sent` et son `quote_snapshot`, seule preuve de ce qui a été transmis au client). `qa-review` a explicitement tranché la question laissée ouverte en §8.12bis décision 7 : fermer la moitié `UPDATE` sans fermer la moitié `DELETE`, plus destructrice, n'était pas défendable — à clore dans le même lot.
  - **Correction.** La même fonction porte désormais aussi un `BEFORE DELETE` (`before update or delete on commercial_quotes`) : refuse un `DELETE` direct quand `old.status <> 'draft'`, sans aucune échappatoire (aucune fonction de ce contrat n'a besoin de supprimer un devis non-brouillon). Piège explicitement évité — même modèle que `commercial_quote_lines_require_draft_quote()` (E10.9, correctif N1) : `commercial_quotes.tenant_id` porte `on delete cascade`, donc une garde naïve aurait rendu impossible la suppression d'un tenant entier dès qu'il porte un devis `sent`. La branche DELETE distingue la cascade (le tenant parent, `old.tenant_id`, a déjà physiquement disparu au moment où ce trigger se déclenche sur le devis) d'un appel direct (le tenant existe toujours) et laisse passer la première. Testé : `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` scénario 3quater (DELETE direct d'un devis `sent` par un membre ordinaire refusé, lignes et journal d'entête intacts en nombre avant/après) et scénario 10 (suppression d'un tenant entier portant un devis `sent` réussit, cascade non bloquée).
- **Nit N3.** `tests/contract/_fakes/commercial-quotes-repository.fake.ts:39` et `:491` citaient encore la migration inexistante `20260906000100` au lieu de `20260906160000` — corrigé aux deux occurrences.

**Dette réaffirmée après ce round.** Le fichier `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` porte maintenant 4 rounds de scénarios accumulés (round 1 : B1/B2/B3 ; round 1 suite : 3ter/status_forced ; round 2 : 3quater/5bis/10) et n'a **toujours jamais été exécuté réellement** (Docker absent des postes utilisés jusqu'ici) — voir **u1**. Chaque scénario a été relu ligne à ligne avec un soin croissant à mesure que le fichier grossit, mais une relecture n'est pas une exécution : la première ouverture d'un poste équipé Docker doit lancer `pnpm test:storefront:sql` sur ce fichier avant tout autre travail dessus.

**Corrigé au round `qa-review` 3, bloquant B6.** Le correctif B4 ci-dessus (`on delete set null` sur `source_quote_id`) rouvrait le MÊME 500 permanent par une autre porte, exactement le genre d'erreur que ce document existe pour empêcher de répéter.

- **Bloquant B6.** `on delete set null` n'est **pas** silencieux en Postgres : l'action RI est implémentée par un vrai `UPDATE ... SET source_quote_id = NULL WHERE source_quote_id = <A>` exécuté SUR LA TABLE RÉFÉRENÇANTE — ici `commercial_quotes` elle-même, puisque la colonne référence sa propre table. Cet `UPDATE` déclenche donc les triggers utilisateur de la ligne modifiée (la copie), exactement comme n'importe quel autre `UPDATE`. Scénario, 100 % API publique, aucun appel PostgREST direct : (1) devis A `draft` créé ; (2) `POST /quotes/{A}/duplicates` → copie B, `source_quote_id = A` ; (3) `POST /quotes/{B}/send` → B passe `sent` (le geste que cette story livre) ; (4) `DELETE /quotes/{A}` — A est `draft`, la garde CA6 et la branche `DELETE` du trigger d'immuabilité (posée pour B5) laissent passer la suppression d'A → l'action RI `on delete set null` déclenche un `UPDATE` sur B pour lui mettre `source_quote_id = null` → B n'est PLUS `draft` → le trigger d'immuabilité (branche `UPDATE`, posée pour B3) lève `quote.update_requires_draft` → `remove()` ne mappe pas cette exception SQL en erreur de domaine → 500 `api.internal_error`, A de nouveau indélébile pour toujours. Symptôme identique à B4, avec `P0001` au lieu de `23503`. Aucun test ne l'attrapait : le scénario SQL 5bis et le test de contrat B4 laissaient la copie en `draft` (jamais envoyée), et le faux de test nullait `source_quote_id` inconditionnellement sans jamais vérifier le statut de la copie — le faux ne reproduisait donc pas le comportement réel à l'endroit précis qui comptait.
  - **Correction (option la plus économique, retenue).** Retrait pur et simple de la contrainte de clé étrangère sur `source_quote_id` : la colonne reste un `uuid` nu, sans `references`. Plus d'action RI, plus de trigger déclenché par cette voie, plus de 500 possible par cette porte. Cohérent avec le commentaire de colonne, inchangé depuis le lot initial (« filiation informative, aucune synchronisation ») : cette filiation n'a jamais prétendu garantir que l'original existe encore ; la copie garde désormais un identifiant **orphelin** si l'original est supprimé, ce que cette sémantique purement informative admet déjà par construction. Corrigé directement dans `20260906160000...sql` (toujours non déployée, dette **u1** inchangée — « une migration déployée ne change jamais » ne s'applique pas ici).
  - **Faux de test aligné.** `commercial-quotes-repository.fake.ts`, `remove()` : la boucle qui nullait `source_quote_id` des copies en mémoire est **retirée** — sans FK, plus rien ne le fait en base, le faux ne doit donc plus simuler un comportement qui n'existe plus.
  - **Testé.** `tests/contract/commercial-quotes.contract.test.ts` : le test B4 existant est révisé (la copie garde désormais `source_quote_id` intact après suppression de l'original, au lieu de le voir nullé) ; nouveau test « B6 — supprimer A dont la copie B a été ENVOYÉE » — vérifie que la suppression d'A réussit (200) ET que B garde `source_quote_id = A` intact après, `status` toujours `sent`. `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` : scénario 5bis révisé dans le même sens (assertion `NULL` remplacée par assertion « intact ») ; nouveau scénario **5bis-b** — duplique un devis, ajoute une ligne à la copie, envoie la copie (`draft` → `sent`), supprime l'original : doit réussir sans exception, `source_quote_id` de la copie reste intact.
  - **Vérification demandée par `qa-review`, faite.** Grep de tous les usages de `source_quote_id` dans `src/` : la seule lecture applicative (`commercial-quotes-repository.ts:596`, `toQuoteDto`) est une passe-through directe (`row.source_quote_id ?? null`), sans jointure ni hypothèse de validité. Aucun autre endroit du code ne suppose que cette colonne désigne encore une ligne existante.

**Dette réaffirmée après ce round.** Le fichier `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` porte maintenant 5 rounds de scénarios accumulés (round 1 : B1/B2/B3 ; round 1 suite : 3ter/status_forced ; round 2 : 3quater/5bis/10 ; round 3 : 5bis révisé/5bis-b) et n'a **toujours jamais été exécuté réellement** (Docker absent des postes utilisés jusqu'ici) — voir **u1**. Le nouveau scénario 5bis-b a été relu ligne à ligne avec la même rigueur que les précédents ; c'est désormais la seule chose qui manque avant un `Approved` possible.

**Round `qa-review` 4 : `Approved`, dette u4 tracée** (voir la ligne u4 ci-dessus) — aucun bloquant nouveau.

**Round `qa-review` 5 — bloquant B7, trouvé par la PREMIÈRE exécution réelle du fichier** (Colima installé en session, `pnpm db:local:reset && pnpm test:storefront:sql` enfin joignables — clôture de fait de la dette **u1** pour ce fichier précis).

- **Bloquant B7.** `api_send_commercial_quote` pose l'échappatoire d'immuabilité `perform set_config('magrit.quote_transition', 'true', true);` (section 4, avant ses deux branches) mais ne la remettait **jamais** à vide avant son unique point de sortie (`return p_quote_id;`). Le raisonnement du round 1 (« la clause `set search_path = public` de la fonction déclenche `AtEOXact_GUC()` à la sortie, qui restaure tout GUC posé pendant l'exécution ») était **faux** : cette clause ne restaure QUE le paramètre qu'elle nomme explicitement (`search_path`), aucun autre. `set_config(nom, valeur, true)` a la sémantique de `SET LOCAL` — porté par la **transaction** englobante, pas par la fonction — et survit donc à la sortie de la fonction pour tout le reste de la transaction. Reproduit isolément (session psql, transaction unique) : après un appel réussi, `current_setting('magrit.quote_transition', true)` valait encore `'true'`, et un `UPDATE ... set status = 'draft'` sur ce même devis, dans la MÊME transaction, passait sans être bloqué par le trigger d'immuabilité (section 2ter) — exactement le trou que B3 (round 1) visait à fermer, rouvert par une autre porte. Sans impact pour un appel PostgREST ordinaire (une requête HTTP = une transaction, close avec elle), mais un piège réel pour tout futur enchaînement dans une même transaction (envoi groupé appelant cette fonction en boucle, script privilégié multi-opérations).
  - **Correction.** Remise à vide explicite de `magrit.quote_transition` **et** `magrit.change_set_id` (posée à la même ligne ~634, même sémantique) juste avant le `return`, sur les deux branches (premier envoi et renvoi). `nullif(current_setting(..., true), '')::boolean` (logique déjà en place dans le trigger d'immuabilité et le trigger d'audit) traite une chaîne vide exactement comme `NULL` → `coalesce(v_transition, false)` → `false` : le trigger redevient actif immédiatement après le retour de la fonction. Corrigé directement dans `20260906160000...sql` (toujours non déployée, dette **u1** inchangée pour le reste des scénarios non encore exécutés sur un vrai Supabase distant — seule l'exécution **locale** vient d'avoir lieu).
  - **`magrit.change_set_id` : même geste, motif différent.** Ce n'est **pas** un échappatoire de sécurité (aucune garde à contourner) — c'est un identifiant de corrélation d'audit, consommé par `commercial_quotes_write_header_audit()`/`commercial_quote_lines_write_audit()`, qui retombe sur un `gen_random_uuid()` frais si absent/vide. Une valeur résiduelle ne permet donc de contourner aucune garde, mais fausserait le regroupement d'une écriture **sans rapport** survenant plus tard dans la même transaction (elle hériterait à tort du `change_set_id` d'un envoi précédent). Remis à vide par cohérence et défense en profondeur, même si le risque est un défaut d'intégrité du journal, pas une brèche de sécurité.
  - **Recherche du même motif ailleurs dans le dépôt (demandée).** `grep -rn "set_config(" supabase/migrations/*.sql` : trois sites au total, tous déjà cités — `api_send_commercial_quote` (`magrit.quote_transition` + `magrit.change_set_id`, ce lot) et deux fonctions d'E10.9 (`api_delete_commercial_quote_line`, `api_reorder_commercial_quote_lines`, migration `20260904000100`), qui posent `magrit.change_set_id` selon le même motif (`set_config(..., true)`, jamais remis à zéro). **Aucun autre échappatoire de sécurité de ce type dans le dépôt** — ces deux fonctions ne portent aucune garde à contourner, seulement la même corrélation d'audit que ci-dessus.
  - **Correction — erreur de process détectée et corrigée après le verdict qa-review round 5.** Le premier correctif avait été écrit directement dans `20260904000100_gescom_e10_9_quote_line_discounts.sql`, sur la foi d'une vérification incorrecte (« jamais remontée dans `main`, donc jamais déployée » — vrai sur `main`, faux en réalité : `supabase migration list --linked` confirme que cette migration est **déjà appliquée** sur le projet Supabase partagé, poussée depuis cette branche avant ce round, indépendamment de tout merge Git). Une migration déployée ne change jamais après coup — règle rappelée par elle-même. Le fichier `20260904000100...sql` a été restauré à son contenu exact d'avant ce lot (`git checkout 57ec6f1 --`), et le correctif est porté par une migration additive séparée : `20260906185313_gescom_e10_9_fix_change_set_id_guc_leak.sql` (`create or replace function`, signature inchangée, aucun impact sur les appelants). Rejoué en entier via `pnpm db:local:reset` : les deux migrations s'enchaînent proprement, `tests/sql/gescom-e10-10a-quote-send-duplicate.sql` et `tests/sql/gescom-e10-11-can-manage-pricing.sql` repassent intégralement (`EXIT=0`). **Leçon** : vérifier l'état déployé réel (`supabase migration list --linked`) avant de modifier un fichier de migration existant, pas seulement son statut de merge Git — les deux peuvent diverger sur une branche de longue durée qui pousse ses propres migrations avant tout merge vers `main`.
  - **Effet de bord découvert en conséquence, corrigé dans la foulée (hors B7, même famille).** Le fichier `tests/sql/gescom-e10-9-quote-line-discounts.sql` (scénario de scaffolding « ligne devenue non-brouillon ») faisait un `UPDATE public.commercial_quotes SET status = 'sent'` **direct**, écrit avant que le trigger d'immuabilité de B3 (round 1 de CE lot, postérieur à l'écriture du fichier E10.9) n'existe. Rejeté par ce trigger à la première exécution réelle combinée des deux migrations. Corrigé en posant/refermant la même échappatoire `magrit.quote_transition` autour de ce seul `UPDATE` de scaffolding — refermée **immédiatement**, sans quoi les rejets attendus juste après (UPDATE/DELETE de ligne sur ce même devis, qui doivent échouer) auraient cessé de se produire : une illustration directe, dans un fichier de test, du même défaut que B7.
  - **Testé.** `tests/sql/gescom-e10-10a-quote-send-duplicate.sql`, nouveau scénario **2bis** : vérifie que `current_setting('magrit.quote_transition', true)` et `current_setting('magrit.change_set_id', true)` sont bien vides/`NULL` immédiatement après le retour d'un appel réussi à `api_send_commercial_quote` — test de la fuite elle-même, pas seulement de son symptôme (déjà couvert par le scénario 3bis, qui échouait avant ce correctif). Exécution réelle (`pnpm db:local:reset && pnpm test:storefront:sql` ciblé sur ce fichier, Colima) : **passe intégralement**, ainsi que `gescom-e10-9-quote-line-discounts.sql` après son propre correctif.
  - **Dette découverte en marge, hors périmètre de ce round (non corrigée ici).** La première exécution réelle combinée de plusieurs fichiers `tests/sql/` révèle deux problèmes **sans rapport** avec B7 : (1) `gescom-e10-3-commercial-quotes.sql` et `gescom-e10-9-quote-line-discounts.sql` échouent tous deux sur `protect_last_tenant_admin` (trigger de la migration `20260824000200_um1_admin_shop_guards.sql`, chantier UM1, antérieure à E10.9) — leur phase RLS retire la seule qualité d'admin de l'acteur de test sur le tenant A pour simuler un membre dégradé, ce que ce trigger refuse désormais inconditionnellement (aucun autre admin du tenant n'a été créé dans ces fichiers) ; (2) `gescom-e10-6-price-rules.sql` échoue sur une assertion de journal d'audit (« désactivation seule journalisée comme "created", "deactivated" attendu ») sans rapport avec des GUC. Signalé pour triage séparé, **pas corrigé** dans ce lot (scope strictement B7) : `tests/sql/gescom-e10-11-can-manage-pricing.sql` et `tests/sql/gescom-e10-7-price-rules-resolve.sql`, en revanche, passent intégralement en exécution réelle.

## 9. Commandes

```bash
pnpm gen:api               # regénère les types depuis le contrat
pnpm gen:api:check         # échoue si les types générés ont dérivé du contrat
pnpm typecheck             # tsc sur kernel + platform + modules + adapters + server/api
pnpm test:architecture     # frontières modulaires, dont le socle E10
pnpm test:contract         # conformité du contrat et du code au contrat
pnpm test:storefront:sql   # comportement réel en base : triggers, RLS, append-only
```

Les quatre premières tournent en CI sur toute PR vers `main` ([`.github/workflows/architecture.yml`](../../.github/workflows/architecture.yml)) et sont **bloquantes**.

### Ce qui est vérifiable localement, et ce qui ne l'est pas

| Maillon | Vérifiable ici ? | Par quoi |
|---|---|---|
| Aiguillage entre les deux façades | **oui** | `tests/server/api/api-facade-router.test.ts` — façades bouchonnées, on observe laquelle reçoit la requête |
| Refus des collisions de chemin | **oui** | même fichier, `assertNoFacadeCollision` |
| Composition réelle (routes E10.4 montées, tenant du jeton, formats de réponse des deux façades) | **oui** | `tests/server/api/magrit-api-composition.test.ts` |
| Câblage des adaptateurs dans l'edge function | **non** | fichier hors `tsconfig`, exécution Deno requise (§8.2 M1) |
| Exécution bout en bout (`pnpm dev:b5` → clic « Clients » → réponse) | **non** | demande un déploiement Supabase Functions, donc Docker — absent de la machine de développement |
| Comportement réel en base (triggers, RLS) | **non** | `pnpm test:storefront:sql`, Docker requis (§8.1 B3) |

`pnpm test:storefront:sql` exige **Supabase local démarré** (`pnpm db:local:start`, donc Docker) : il exécute les cas de `tests/sql/` par `psql` contre la base réelle. Il n'est pas dans le workflow CI actuel, qui n'a pas de service Postgres. C'est le seul moyen de tester un trigger ou une policy pour de vrai — relire le texte d'une migration ne prouve rien, puisqu'une migration ne change jamais après coup.
