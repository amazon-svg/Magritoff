/**
 * Supabase project credentials — v3 (multi-tenant).
 *
 * ⚠️  Beta 3 tourne sur un PROJET SUPABASE DEDIE, distinct de celui utilise
 *     par Beta 1 / Beta 2 (`jynxrpzwgzrrfuooputw`). Raison : la refonte
 *     multi-tenant touche 100 % des tables et des RLS. Garder v3 isole
 *     evite tout risque sur les demos v1/v2 en cours.
 *
 * Rempli des qu'Arnaud a cree le projet v3 et genere son anon key.
 */

// TODO(v3): remplacer par l'id du nouveau projet Supabase Beta 3.
export const projectId = "__V3_PROJECT_ID_PLACEHOLDER__";

// TODO(v3): remplacer par l'anon key du nouveau projet Supabase Beta 3.
export const publicAnonKey = "__V3_ANON_KEY_PLACEHOLDER__";
